# Progress Log: SmolVLA Hidden-State Causal Analysis

## Session: 2026-07-23

### Phase 1: Inventory and evidence audit
- **Status:** complete
- **Started:** 2026-07-23
- Actions taken:
  - Interpreted the request as both a filesystem separation and an expansion from predictive analysis to causal research.
  - Initialized a persistent scoped plan.
  - Recorded known limitations of the completed hidden-state experiment.
  - Inventoried all SmolVLA-named paths under the user home directory.
  - Measured the main experiment tree (41 GB), LingBot-local environment (11 GB), LeRobot checkout (157 MB), and two obsolete small virtual environments.
  - Confirmed that a usable Miniconda environment already resides outside LingBot.
  - Audited the canonical final report, including benchmark, paired perturbation, hidden-state, uncertainty, and limitation sections.
  - Identified the central evidence gap: the experiment predicts eventual timeout from pooled representations but cannot yet localize modality-specific or causal mechanisms.
- Files created/modified:
  - `.planning/.active_plan`
  - `.planning/smolvla_causal_analysis/task_plan.md`
  - `.planning/smolvla_causal_analysis/findings.md`
  - `.planning/smolvla_causal_analysis/progress.md`

### Phase 2: Safe repository separation
- **Status:** complete
- Actions taken:
  - Created `/home/zhongzhengyang/smolvla-hidden-state-analysis` from the tested standalone Git repository.
  - Atomically moved the 41 GB full experiment tree, LingBot-local Conda environment, legacy virtual environments, LeRobot checkout, dependency log, and setup planning records outside LingBot.
  - Preserved the independently installed Miniconda environment in place as the working runtime.
- Files created/modified:
  - `/home/zhongzhengyang/smolvla-hidden-state-analysis/archive/full_experiment`
  - `/home/zhongzhengyang/smolvla-hidden-state-analysis/local`
  - `/home/zhongzhengyang/smolvla-hidden-state-analysis/vendor`
  - `/home/zhongzhengyang/smolvla-hidden-state-analysis/logs`
  - `/home/zhongzhengyang/smolvla-hidden-state-analysis/planning/initial_setup`

### Phase 3: Research question expansion
- **Status:** complete
- Actions taken:
  - Implemented and ran an offline warning-confound audit on all 400 benchmark episodes.
  - Quantified task-attributable score variance, within-task pairwise discrimination, task-centered metrics, per-suite metrics, and step-0-to-step-100 score growth.
  - Discovered that step-0 hidden-state performance is mostly task identity, while strong episode-specific separation emerges by step 100.
  - Defined modality-specific questions, a causal graph, seven competing explanations, controls, falsification criteria, and a claim ladder from descriptive association to general causal mechanism.
- Files created/modified:
  - `/tmp/audit_warning_confounds.py` (candidate repository script)
  - `/tmp/smolvla_warning_audit/*.csv` (candidate derived tables)

### Phase 4: Implement research design artifacts
- **Status:** complete
- Actions taken:
  - Added a detailed causal research programme and staged experiment design.
  - Added a machine-readable causal validation matrix.
  - Added the reproducible warning-confound audit and its derived tables.
  - Updated the README and package name for the standalone project.
- Files created/modified:
  - `docs/causal_research_program.md`
  - `configs/causal_validation_matrix.yaml`
  - `scripts/audit_warning_confounds.py`
  - `reports/warning_confound_audit/*`
  - `README.md`, `.gitignore`, `pyproject.toml`

### Phase 5: Verification and delivery
- **Status:** complete
- Actions taken:
  - Committed the causal-analysis documentation, configuration, code, and derived results as `4910eff`.
  - Pushed the commit to `Dz803/smolvla-hidden-state-analysis` on GitHub.
  - Fast-forwarded the 51 GB standalone workstation copy to the same commit.
  - Re-ran tests from the separated path and verified that LingBot contains no SmolVLA material except this temporary active planning record.
- Files created/modified:
  - `/home/zhongzhengyang/smolvla-hidden-state-analysis`
  - GitHub `main` at commit `4910eff9b5755ba7deb0076987f46870b3644f2e`

## Test Results
| Test | Input | Expected | Actual | Status |
|------|-------|----------|--------|--------|
| Planning initialization | Scoped planning files | New active plan | Created | Pass |
| SmolVLA inventory | Named paths and disk sizes | Complete ownership map | 41 GB experiment plus LingBot-local support paths identified | Pass |
| Atomic relocation | Seven exact LingBot-local paths | All absent from LingBot and present in standalone root | Verified | Pass |
| Warning confound audit | 400-episode benchmark probe predictions | Separate task versus episode-specific signal | Step-0 near-chance within task; strong hidden-state separation by step 100 | Pass |
| Standalone unit tests | `PYTHONPATH=src ... python -m pytest -q` | Existing tests pass after separation | 3 passed | Pass |
| Audit reproducibility | Rerun and SHA-256 compare three CSVs | Byte-identical outputs | All hashes matched | Pass |
| Final standalone tests | Separated path with explicit `PYTHONPATH` | Tests run without LingBot files | 3 passed in 1.66s | Pass |
| Git synchronization | Compare `HEAD` and `origin/main` | Identical commit | Both `4910eff9...` | Pass |
| LingBot residue scan | Find `*smolvla*` to depth 5 | Only active plan remains before archival | Exactly one planning path | Pass |

## Error Log
| Timestamp | Error | Attempt | Resolution |
|-----------|-------|---------|------------|
| 2026-07-23 | `/home/zhongzhengyang/miniconda3/envs/smolvla-libero` cannot import pandas | 1 | Switched to relocated `local/lingbot-conda-env-archived`, which loaded all Parquet artifacts successfully |
| 2026-07-23 | `git clone --local` failed with cross-device hard-link error | 1 | Use `git clone --no-hardlinks` for the small standalone repository |
| 2026-07-23 | Git commit could not infer author identity in the fresh clone | 1 | Configure repository-local `zhongzhengyang` no-reply identity before retrying |
| 2026-07-23 | Push targeted checked-out local repository and was refused | 1 | Point temporary clone `origin` to `https://github.com/Dz803/smolvla-hidden-state-analysis.git` |

## 5-Question Reboot Check
| Question | Answer |
|----------|--------|
| Where am I? | Complete |
| Where am I going? | User handoff; next execution is the two-part causal gate |
| What's the goal? | Standalone project and explanatory causal programme |
| What have I learned? | Step-0 is mostly task confounding; trajectory-specific hidden-state signal emerges by step 100 |
| What have I done? | Relocated 51 GB, added and verified causal audit/design, synchronized GitHub |

## Session: 2026-07-27 — Resumability hardening

- **Status:** complete
- Audited the README, local planning records, Git state, and planning-tool discovery from a fresh-project perspective.
- Found that the completed plan existed only in ignored `planning/causal_analysis`, so a new session or another device could not automatically discover it.
- Added tracked `.planning/.active_plan`, a forward-looking causal-validation task plan, durable findings, and the complete progress history.
- Added `AGENTS.md`, `docs/resume.md`, `docs/experiment_log.md`, and `scripts/resume_check.sh`.
- Updated obsolete LingBot-relative resource paths and documented the correct offline Python and canonical benchmark paths.
- Verified `scripts/resume_check.sh --full` in a code-only checkout: all tracked resume files were present and local-only assets were correctly reported as informational rather than failures.
- Verified shell syntax and Git whitespace checks.
- Fast-forwarded the workstation project to resume-infrastructure commit `ef81cb6` and confirmed `HEAD == origin/main`.
- Ran the full resume check from `/home/zhongzhengyang/smolvla-hidden-state-analysis`: active-plan discovery, core imports, canonical benchmark presence, and all 3 tests passed (`resume_check=PASS`).
- Resume point: Phase 1 of `.planning/causal_validation/task_plan.md`; do not rerun completed canonical rollouts.

## Session: 2026-07-27 — Phase 1 offline robustness gate

- **Status:** complete
- Ran `bash scripts/resume_check.sh --full`; active-plan discovery, archived Python, imports, canonical benchmark, and 3/3 pre-existing tests passed.
- Extended `scripts/audit_warning_confounds.py` with deterministic episode and whole-task cluster intervals, suite-held-out probes, task-held-out cross-condition probes, explicit non-estimability reporting, and a five-sample state/action-history baseline.
- Added reusable transfer/history utilities in `src/smolvla_analysis/robustness.py` and four focused tests in `tests/test_robustness.py`.
- Verified a 10-draw end-to-end smoke analysis in `/tmp/smolvla_phase1_smoke` before starting the canonical derived run.
- Interrupted one over-broad 2,000-draw pass before it wrote outputs because it was resampling every exploratory layer probe. Restricted intervals to the six predeclared Phase 1 features while retaining all exploratory point estimates; reran successfully with 2,000 draws.
- Completed the offline gate using the immutable 400-episode benchmark and the four existing 90-episode clean/blur/paraphrase/wrist-mask runs. No rollout or archived artifact was modified.
- Derived output contract passed: 51 confound rows, 72 held-out-suite rows, 108 cross-condition rows, no duplicate evaluation keys, complete primary-feature intervals, and explicit zero coverage for wrist-mask-to-clean fitting.
- Scientific decision: the step-100 hidden-state advantage survives bootstrap, held-out-suite, and richer-history controls. Action-expert transfer survives all estimable clean↔perturbation directions; VLM paraphrase transfer is weaker; wrist-mask-to-clean is not estimable because the source has no successful episodes.
- Created `reports/offline_robustness_gate/` with five CSV tables and an interpretation README.
- Final focused/full tests: 4/4 and 7/7 passed; `git diff --check` passed.
- Error recorded: the first inline schema-inspection command used literal escaped newlines and raised `SyntaxError`; replaced it with a newline-free expression.
- Error recorded: the first canonical bootstrap scope was computationally excessive; stopped it before artifact creation and narrowed bootstrap uncertainty to the predeclared features without changing the 2,000-draw contract.
- Resume point: Phase 2, failure semantics and token instrumentation.

## Session: 2026-07-27 — Broad blind-spot audit

- **Status:** complete
- Re-ran the full resume check; 7/7 tests passed and canonical evidence remained available.
- Began a research-design stress test before Phase 2 in response to the request for a broader, model-agnostic research direction.
- Audited SmolVLA execution and activation-capture code. Confirmed 50-step queued open-loop execution and post-episode activation recomputation at every fifth saved observation.
- Confirmed that post-hoc activation recomputation draws fresh flow-matching noise and averages action-expert activations across action tokens and denoising invocations.
- Preliminary implication: archived activations are useful offline diagnostic representations but are not the exact online hidden states that generated the recorded actions. The existing policy-output baseline is also misaligned because it summarizes the original shrinking action queue, not the fresh action chunk associated with the archived activation.
- Next: quantify replanning/termination confounds and review primary papers/official implementations before revising the experimental programme.
- Quantified the action-queue cadence from 89,064 step records: inference latency is about 120.6× higher at multiples of 50, confirming true online replanning only at those boundaries.
- Quantified fixed-horizon survivor bias: 81 successes are already complete by step 100, while all 145 failures remain active. Recomputed exact-step landmark metrics on the 319 active episodes; hidden-state ranking remains strong, so the flaw changes but does not erase the result.
- Added the previously missing ordered-action baseline using complete 50×7 chunks at replanning boundaries. Its step-100 AUPRC is 0.791 and within-task AUROC is 0.805, closing most of the gap to the action expert (0.851/0.841).
- Began the external primary-source review. Initial relevant work includes official SmolVLA architecture documentation, LIBERO-CF counterfactual language-following evaluation, and ICBench contradiction tests; these motivate symmetric tests for vision and language shortcuts.
- Expanded the review to AC-VLA, LIBERO-Para, and ProbeAct. Their reported trajectory-overfitting, wrist-view shortcut, lexical sensitivity, and explicit kinematic failure-detection results directly motivate stronger action-plan, compositional-language, and semantic-decoding controls.
- Added probe-validity and causal-method checks from control-task selectivity, conditional probing beyond a full ordered action-plan baseline, causal abstraction/DAS/interchange interventions, and exact verification after approximate patch-site screening.
- Search/tool error: a broad survival/landmark-analysis query produced truncated, unusable output. It will be replaced by narrower primary-source searches and is not being treated as evidence.
- Editing errors: two append attempts used stale or cross-file context and did not apply. Re-read the exact section tails and applied a targeted patch; neither failed attempt changed a file.
- Reviewed official benchmark/code evidence for LIBERO-PRO and recent open-loop VLA correction methods. Added a central distinction: probe task holdout does not imply policy OOD generalization, while LIBERO-PRO directly exposes memorized trajectory/layout behavior through object, position, language, task, and environment shifts.
- Identified the 50-step action queue as both a mechanistic confound and an intervention variable: compare fixed horizons or event-triggered replanning before attributing warning signal to semantic understanding.
- Inspected the feature-construction code and confirmed that the historical policy-output control discards the complete temporal order of the action chunk.
- Tooling issue: `rg` is unavailable in the current shell. Switched to read-only `find`/`grep`; a piped `sort | head` inspection then emitted a harmless broken-pipe warning when `head` closed after its requested lines. No files changed.
- Confirmed the immutable benchmark exposes 89,064 step rows with complete executed actions and queued action chunks, making a stronger offline sequence/template control feasible without rerunning rollouts.
- Audited archived semantic state: `object_state` is null for every step, `goal_state` only records the normalized-progress fallback, and all phase labels are time-bin aliases rather than physical manipulation events. This elevates privileged event instrumentation to a prerequisite for semantic mechanism claims.
- Compared the resolved recording contract with artifacts: `save_environment_state` was enabled but the environment/object state is absent. Flagged this as an instrumentation bug to repair before causal branching or phase semantics.
- Checked local training-data availability. No accessible demonstration corpus is present under `checkpoints/libero_datasets`, so direct training-trajectory nearest-neighbor memorization must be deferred or use an external dataset snapshot; evaluation-trajectory clustering alone cannot prove training memorization.
- Predeclared the reproducible trajectory-confound comparison: exact steps 0/50/100, active episodes only, common task-held-out folds, ordered action prefix/current chunk/joint controls, and blockwise fold-local conditional probes with discrimination and calibration metrics.
- Added `scripts/audit_trajectory_confound.py` and two focused tests. The audit excludes the landmark action from history, preserves full chunk order, applies scaling/PCA within each task-held-out fold and feature block, and reports prevalence-normalized AUPRC, AUROC, within-task AUROC, Brier score, and log loss.
- Focused trajectory-audit tests passed (2/2).
- Error: the first canonical trajectory-audit pass stopped before output because nested Parquet action chunks arrived as object-typed NumPy arrays and direct float conversion failed. Reused the project's established object-array-to-list normalization and added a regression test before rerunning.
- Editing error: the first normalization patch omitted an explicit file boundary before the test edit and did not apply. Reapplied it with exact file targets; the failed attempt changed no file.
- Reran the canonical derived trajectory-confound audit successfully without modifying the archive. Wrote metrics and predictions to `reports/trajectory_confound_audit/`.
- Exact step-100 results on 319 active episodes (five task-grouped folds): behavioral context 0.758 AUPRC / 0.804 AUROC / 0.803 within-task AUROC; action expert 0.866/0.865/0.839; VLM 0.882/0.883/0.813. The ordered action prefix alone is 0.558/0.620/0.653 and the current ordered chunk alone is 0.689/0.723/0.771.
- The blockwise concatenated probes exposed calibration/regularization instability (log losses above 2 for some step-100 combinations). Marked those conditional proper-score comparisons non-conclusive pending nested task-grouped regularization rather than interpreting degradation as negative information.
- Upgraded every trajectory-confound model to choose logistic regularization from five values using nested task-grouped folds. Scaling and blockwise PCA are refitted inside each inner fold, and the selected value is stored per outer-fold prediction. Focused tests still pass (3/3).
- Completed the nested canonical rerun and replaced the preliminary derived outputs. At step 100, behavioral context reaches 0.803 AUPRC / 0.821 AUROC / 0.803 within-task AUROC / 0.532 log loss. Adding action expert yields 0.882/0.891/0.843/0.435; adding VLM yields 0.883/0.891/0.863/0.434.
- Decision: full behavior and plan explain much of the warning signal, but a measurable hidden-state increment remains. Because no visual or privileged scene-state baseline exists, the increment is evidence for current-observation information—not yet for semantic scene understanding.
- Verified that all episode records reference saved observations and videos. A sampled NPZ contains per-step main/wrist RGB arrays shaped `[T, 3, 256, 256]` plus robot/policy/action arrays, enabling an offline low-resolution visual-progress control without simulation.
- Inspection warning: another `find | head` listing emitted expected broken-pipe messages when `head` closed early; no data or process failed.
- Verified runtime alignment: both cameras are captured from observation `o_t` before policy inference/action `a_t`. Also confirmed `object_state` is unconditionally written as `None`; the missing environment state is a recorder implementation gap rather than an artifact-reading error.
- Inspection error: attempted to read a non-existent `src/smolvla_analysis/storage.py` after the relevant runtime excerpt. The runtime evidence was complete; no file changed.
- Extended the trajectory audit with exact current-frame and initial-to-current visual-change controls using deterministic 16×16 RGB block averages for both cameras, plus visual+behavioral conditional models. Added a pooling regression test.
- Completed the visual-control audit. Step-0 current pixels reach 0.532 AUPRC and 0.607 within-task AUROC, exposing initial layout/seed difficulty. Step-100 current pixels reach 0.721 AUPRC / 0.774 AUROC / 0.731 within-task AUROC; pixel change alone is weaker (0.512/0.612/0.699).
- The visual+behavioral concatenated probes were unstable under held-out-task shift and are not being treated as a decisive conditional test. Standalone pixel results are retained; the next semantic control should use privileged geometry or a frozen representation with block-specific capacity control.
- Numerical warning: PCA reported undefined explained-variance ratios for the all-zero step-0 pixel-change block. Predictions remained finite and exactly at the constant baseline; constant-block handling will be made explicit before final verification.
- Added explicit constant-column handling before PCA and a warning regression test; focused trajectory-audit tests pass (5/5).
- Verified from official Hugging Face sources that the evaluated checkpoint was trained on `lerobot/libero`, whose single training split covers task indices 0–39 and about 1,690 episodes. The current 40-task benchmark is policy in-distribution by task identity even when the downstream probe uses task-held-out folds.
- Reviewed the official LIBERO evaluation contract and the 2026 manipulation-benchmark audit. Recorded that evaluation draws from fixed indexed initial states and that a tiny no-language policy can nearly saturate LIBERO, so the project must distinguish benchmark shortcut performance from grounded/general manipulation capability.
- Reviewed primary language-grounding work: Drop-Then-Recovery (language-path redundancy), Anchor-Align (fine-tuning representation drift/language-action misalignment), ICBench and LIBERO-CF (visual dominance under contradictions), and step-wise multilingual sensitivity. Reframed the language question as conditional causal influence at decision events rather than a single strong-versus-weak prior.
- Reviewed direct neighboring work on frozen-VLA value probes, Hide-and-Seek weakly supervised monitoring, SAFE, ProbeAct, and event-grounded sparse autoencoders. Decided that generic hidden-state failure prediction is no longer a competitive novelty claim.
- Refined the candidate contribution to causal factorization of semantic state, sampled action plan, generic progress, and counterfactual recoverability, using restored-state branching and exact online activations.
- Source-access error: the NeurIPS SAFE PDF exceeded the browser fetch limit. Used its official proceedings search result/abstract for high-level scope only; no unsupported implementation detail was inferred.
- Read the full HTML methods/results for the closest value-probing paper and neighboring event-grounded/weak-label methods. Confirmed direct SmolVLA probing, goal-swap, and simulator snapshot overlap, but also a clear unresolved boundary: single-trajectory outcome targets are not Bellman-consistent and no method decomposes recoverability from plan/noise/modality content.
- Named the candidate research method **Counterfactual Recoverability Decomposition (CRD)** and began specifying its factorial interventions and estimand.
- Examined goal-swap directionality and closed-loop SAE intervention results. Added stricter causal criteria: correct counterfactual direction, matched rescue and induction, dose response, and preserved controller fidelity; generic score movement or destructive ablation does not qualify.
- Re-read the existing causal research programme and resume guide against the new evidence. Determined that the programme requires a substantive phase reorder and resume-document update before Phase 2 begins.
- Selected a concrete shared-scene language/goal smoke pair: LIBERO-Goal tasks 3 and 4 (same bowl; drawer-inside versus cabinet-top), with complementary observed difficulty. Verified that training actions/metadata can likely be audited from the 1.94 GB official dataset without downloading video, but did not download anything.
- Wrote the complete synthesis and falsifiable method specification in `docs/blind_spot_research_audit.md`, including the CRD estimands, hierarchical branch design, cross-goal plan-faithfulness metric, competing-hypothesis signatures, stop conditions, and cost-gated sequence.
- Replaced the obsolete annotation-first continuation in the active task plan with Phase 2 measurement alignment, Phase 3 two-task branched CRD, Phase 4 matched causal intervention, and Phase 5 actual OOD/architecture generalization. Updated `docs/resume.md`, `README.md`, the causal-programme banner, durable findings, and the experiment ledger accordingly.
- Regenerated `reports/trajectory_confound_audit/` after explicit constant-block handling. All headline values reproduced exactly and the previous all-zero PCA warning disappeared.
- Verification error: invoking the audit script directly without `PYTHONPATH=src` raised `ModuleNotFoundError: smolvla_analysis`. Re-ran with the documented project path; no artifact was changed by the failed help command.
- Final verification passed: 12/12 full tests, `bash scripts/resume_check.sh --full`, and the canonical benchmark availability check. No rollout was launched and no file under `archive/full_experiment/runs` was modified.
- Resume point: Phase 2 measurement-alignment and forward-only gate. Gate A uses saved observations and exact fixed-noise forwards; Gate B's approximately 160 branched continuations require explicit approval.

## Session: 2026-07-27 — Cross-architecture overlook-discovery methodology

- **Status:** complete
- Began a primary-source boundary review for a methodology that is not tied to SmolVLA and can later include π0.5 and GR00T.
- Verified that official `openpi` provides π0.5 flow-matching base/LIBERO checkpoints and PyTorch/JAX paths, while official Isaac-GR00T now offers N1.7 and LIBERO checkpoints in addition to the scientifically distinct N1.5 design.
- Identified the first design constraint: architecture comparisons require an environment-level causal interface and a controlled-versus-ecological two-track evaluation, because checkpoint training mixtures and embodiments otherwise dominate any hidden-state comparison.
- Mapped nearby July 2026 work. SVA already distills simulator search into a Q evaluator for frozen VLAs; Reflective VLA already conditions on observation–action–consequence triplets; deployment-time reliability work already covers progress monitors, training influence, and sequence success estimation.
- Narrowed the candidate gap to cross-policy counterfactual competence decomposition and interaction-based overlook discovery, rather than another per-policy failure probe or candidate-action selector.
- Reviewed broader competence and policy-comparison work. Aggregate sequential policy comparison, skill competence estimation, and per-policy foresight values already exist; none found in this search used identical restored manipulation states across several VLA families to separate shared hardness from model-specific recoverability.
- Audited recent diagnostic/failure-discovery benchmarks. RADAR, ForesightSafety-VLA, LIBERO shift suites, and ROBOGATE already cover broad factor taxonomies and adaptive boundary sampling.
- Refined the target from “discover failure regions” to “discover and causally validate policy-by-factor and factor-by-factor interactions at identical physical states.”
- Verified official LIBERO evaluation paths for π0.5 and GR00T N1.7. Both standard checkpoints are near 97% mean success, while native action horizons differ (10 versus up to 16, compared with SmolVLA's 50).
- Added action horizon/replanning cadence as a predeclared causal factor and separated native-system from controller-normalized comparison tracks.
- Found a direct headline collision: *How VLAs Fail Differently* already reports architecture-specific black-box action failure signatures. Restricted the intended contribution to matched-state causal decomposition and interaction confirmation.
- Added executable-policy certificates after reviewing action-metadata failure evidence, and separated native policy stochasticity from externally injected candidate perturbations after reviewing BOKBO.
- Searched candidate terminology and adjacent same-state audit methods. No exact “Cross-Policy Recoverability Decomposition” collision was found, but a recent locomotion adapter audit makes same-state headroom an insufficient novelty claim.
- Considered an item-response formulation for shared state difficulty and policy ability. Rejected unqualified IRT inference because three evaluated policies are a small, clustered system sample; retained paired contrasts and a simulation-calibrated hierarchical binomial model.
- Specified Cross-Policy Recoverability Decomposition (CPRD): a restored-state policy×factor×proposal×continuation outcome design, paired statewise contrasts, a cross-classified binomial decomposition, and model-local self-specificity tests.
- Added an architecture-neutral effect-space comparison so raw action chunks, normalization, and native horizons are not treated as equivalent across SmolVLA, π0.5, and GR00T.
- Defined a sequential overlook-discovery loop using cost-aware fractional screening, policy disagreement and posterior residual acquisition, typed counterfactual interventions, and independent held-out confirmation.
- Defined ecological versus controlled post-training tracks and a version-pinned baseline recommendation. No checkpoint, dataset, rollout, or immutable artifact was downloaded or modified.
- Kept the active Phase 2 unchanged pending user approval; the CPRD extension is recorded as a candidate methodological decision rather than silently replacing the execution plan.

## Session: 2026-07-27 — Phase 2 measurement-alignment implementation

- **Status:** in progress
- User approved proceeding with Phase 2 and requested latent/modality, VLM-to-flow, trajectory-memorisation, and cross-policy self-knowledge investigations.
- Scope is limited to the existing SmolVLA checkpoint and small deterministic/offline checks. No canonical rollout will be repeated, no external checkpoint will be downloaded, and `archive/full_experiment/runs` remains immutable.
- Success gates: exact action-producing-forward capture; modality/action-position/denoising-step preservation; restorable privileged simulator state; fixed-noise counterfactual queries; and hook-fidelity/within-state-noise validation.
- Tooling error: `rg` is unavailable on this workstation (`rg: command not found`). Continuing with `find`, `grep`, and targeted module inspection rather than retrying the same command.
- Inspection error: the repository has no `experiments/` directory despite it being included in a broad file-list command. The relevant runtime is under `src/`, `scripts/`, and the archived local environment; no file or experiment was affected.
- Planning update error: the first patch used insufficiently specific context for a repeated `**Status:** pending` line and did not apply. The retry targets the Phase 2 heading explicitly.
- Findings-update error: an attempted contextual append targeted wording that exists in `progress.md` rather than `findings.md`; no file changed. Re-read the file tail and appended the instrumentation audit at its actual final section.
- Inspected `activation_hooks.py`, `runtime.py`, the schema/recorder, configuration validation, and existing hook-equivalence test. Confirmed the exact post-hoc re-query, over-pooling, missing query metadata, and missing simulator-state mechanisms that Phase 2 must replace.
- Audited the vendored SmolVLA inference implementation and LIBERO wrapper APIs. Identified the exact one-prefix/ten-denoising invocation structure, deterministic prefix token ordering, and available flattened MuJoCo state restore primitives.
- Runtime-resolution errors: the project root has no `checkpoints/` or `.conda-envs/smolvla-libero/` directory, and the archived offline Python cannot import LeRobot. The checkpoint and processor assets are actually under `archive/full_experiment/checkpoints/`; the policy-capable environment must be resolved separately before executing Phase 2 GPU diagnostics.
- Located `/home/zhongzhengyang/miniconda3/envs/smolvla-libero/bin/python`, but it cannot import Torch in its current state. This confirms the previously documented environment is incomplete, now for policy inference as well as pandas; continuing to search for the original policy-capable runtime instead of installing or downloading dependencies without approval.
- Confirmed the local checkpoint contract and weight presence without loading the model: 50 action positions, ten denoising steps, cached cross-attention, two image spans followed by language and state, and a 907 MB local safetensors file.
- Environment audit: the minimal `smolvla-libero` Conda environment contains only packaging tools. The `lingbotvla` environment imports Torch and LeRobot but not LIBERO, while the relocated archived environment contains Torch and LIBERO but not LeRobot. The next non-mutating attempt is to combine the archived runtime with the vendored LeRobot source through `PYTHONPATH`.
- Confirmed the combined archived runtime plus vendored LeRobot source imports Torch, LeRobot, and LIBERO successfully. CUDA and `nvidia-smi` are unavailable inside the current sandbox, so implementation and CPU unit tests can proceed before requesting a narrowly scoped GPU diagnostic.
- Added `src/smolvla_analysis/phase2_capture.py`. It captures the exact queue-filling `select_action` forward under explicit fixed noise, intercepts the exact chunk, preserves prefix/action token axes and ten denoising calls, records `x_t`/velocity, and summarizes the actual KV cache passed from VLM to action expert.
- Added two focused toy-policy tests covering exact chunk alignment, token spans, flow-step structure, KV-cache summaries, deterministic noise, and the empty-queue guard. Result: 2/2 passed.
- Added `src/smolvla_analysis/libero_state.py` with duck-typed LIBERO state capture/restoration, object geometry/joints, goal-predicate truth, raw contacts, robosuite grasp status, and a strict round-trip validator.
- Added a fake-LIBERO round-trip regression test. Combined Phase 2 focused tests pass: 3/3.
- Added immutable Zarr writers for structured action queries and simulator snapshots. They preserve the explicit invocation/batch/token-or-action/hidden axes, denoising trajectories, KV-cache token norms, exact noise/chunks, and semantic snapshot metadata.
- Added storage regression tests; combined Phase 2 focused tests pass: 5/5.
- Runtime-integration patch error: the first large patch used overlapping inspection output as context and did not apply. Split the change into small exact patches; the failed attempt changed no file.
- Updated the future rollout schema and runtime: every queue-filling forward now receives a stable explicit noise seed, captures the exact action-producing tensors/chunk, and links all queued actions by query ID and action index. The old after-episode stochastic activation re-query has been removed from the active runtime.
- Integrated per-step LIBERO snapshots plus a first-state round-trip gate. Added local-asset fallback resolution so the relocated checkpoint/processor/LIBERO assets can be read from `archive/full_experiment/checkpoints` without modifying them.
- Full unit suite passes after integration: 17/17. `git diff --check` is clean.
- Selected the no-rollout Phase 2 diagnostic cells from existing immutable observations: task-3 success/failure episodes at steps 50 and 100, with task 4 available as a shared-scene alternate goal. Confirmed saved arrays contain both cameras, eight-dimensional policy state, and actions.
- Added `scripts/run_phase2_forward_gate.py`: four saved task-3 states, four proposal seeds, exact-seed repeat checks, fixed-noise paraphrase/alternate-goal/main-view/wrist-view counterfactuals, archive-chunk similarity, modality-resolved VLM changes, KV-cache changes, and denoising/action-expert response curves. Raw tensors are routed to ignored `local/`; only compact derived reports go under `reports/`.
- Script compilation/help, full tests (17/17), and `git diff --check` pass. A one-state GPU smoke is next; it performs no simulator rollout and reads only the existing local SmolVLA checkpoint.
- Completed the one-state real-checkpoint smoke: 9 fixed-observation queries, 40 MB local raw capture, 56 KB report, no simulation. Exact hook fidelity and deterministic repeats both have zero numerical difference.
- Preliminary factor result at task-3 failure step 50: alternate-goal action RMSE 0.859, paraphrase 0.333, main-mean 0.287, wrist-mean 0.295. VLM deltas localize to the manipulated token span, while action-expert/velocity deltas amplify late in denoising.
- Recorded a negative methodological result: KV-cache norm change ranks paraphrase above alternate goal despite the reverse action-effect ranking, so cache magnitude alone is not valid modality attribution.
- Completed the four-state exact-forward confirmation: 36 queries, 158 MB ignored raw capture, 132 KB compact report, no simulator rollout. Hook fidelity and repeat determinism remain exact.
- Raw-analysis error: a substring parser included `repeat_original` groups as ordinary `original` groups and requested a nonexistent noise-202 repeat. No data changed; reran with exact suffix exclusion.
- Added within-state and denoising-time interpretation. The proposal/action-expert coupling is strongest only at the final denoising call and step 100; step-50 coupling is near zero despite comparable hidden variation.
- Discovered and quantified an executable-subspace confound: 25 of 32 internally denoised action channels are discarded in LIBERO, yet carry velocity magnitude and stochastic differences comparable to the seven executed channels. Final padded outputs are correctly near zero, confirming this is denoising work rather than executed behavior.
- Extended exact capture to the input of `action_out_proj`, persisted it in Zarr, and updated regression tests. Full tests remain 17/17 and `git diff --check` passes.
- Completed a one-state action-head subspace diagnostic (9 queries, 46 MB ignored raw data). Noise differences are predominantly output-row/padding aligned (74.4%/65.4%), while controlled modality/goal differences are approximately 96–97% output-null-space with small but strong executed-row projections.
- Completed the four-state action-head confirmation: 36 queries, 183 MB ignored raw capture, 144 KB report, no simulator. The executed/padding/null geometry and direction-selective gain reproduce across success/failure episodes and steps 50/100.
- Ran a primary-source novelty search for VLA action-decoder row/null/Jacobian decompositions. COAST is the closest collision on latent success subspaces and decoding bottlenecks; action-head redesign/injection work is adjacent. No direct VLA executable-versus-padding-versus-null decomposition was found in this search, while output-potent/null geometry is established outside VLA and must be credited rather than claimed as new mathematics.
- Source-access error: one multi-paper browser open returned more content than the tool context limit and was truncated. No claim was based on the truncated response; the relevant papers were reopened individually as searchable arXiv HTML.
- Read COAST's full method and appendices. It preserves denoising steps but mean-pools all action tokens (and all 49 state/future/action tokens for GR00T), fits success/failure geometry from ordinary unmatched rollouts, and reports no decoder/Jacobian, padding, same-state, or flow-noise-controlled decomposition.
- Reviewed the closest new null-space collision, *Output-Level Regularization Eliminates the Seed Lottery*. Its Jacobian null space is in **parameter-update space** during VLA fine-tuning; the present diagnostic is in **inference-time hidden-state space** relative to the executable action/effect decoder. The mathematical motif overlaps, but the estimand and intervention are distinct.
- Found a simulator-side matched-state blind spot: LIBERO's flattened MuJoCo state excludes robosuite controller interpolation state, the Panda gripper's accumulated command, robot history buffers, episode clocks, observable timing, vector-wrapper flags, and RNG state. A physics-only restore is therefore not a valid same-state branch contract.
- Extended `LiberoStateSnapshot` to capture and restore the missing runtime state and added fake-environment regression coverage. Focused state/capture tests pass 5/5.
- State-gate startup error: the first real smoke imported LIBERO before setting `LIBERO_CONFIG_PATH`, triggering its interactive first-run prompt and an EOF. Reordered initialization; no report or simulator artifact was produced.
- State-gate relocation error: the archived LIBERO config contains old absolute workstation paths, so the next attempt could not find an init-state file. The smoke now creates a temporary resolved config pointing to the relocated package and local assets; the archived config remains unchanged.
- State-capture error: LIBERO's `ObjectState.get_joint_state` indexes a free-joint `(start, end)` address as a multidimensional tuple and raised `IndexError`. Added a named-joint fallback using the correct slice semantics, preserving joint values without modifying LIBERO.
- Completed the real task-3 state-contract smoke with one reset and two three-action replay comparisons. MuJoCo-only restoration contaminated the second branch (`0.0372` maximum state difference, camera differences up to `172/255`, and different accumulated gripper commands). Full runtime restoration reproduced final pixels exactly and simulator state to `7.12e-15`; predicates, grasp status, bookkeeping, and RNG also round-tripped exactly.
- Canonical compact state-gate report: `reports/phase2_state_gate/state_contract_20260727T113937Z/summary.json`. This was a tiny deterministic simulator contract test, not a policy rollout; no checkpoint was loaded and no canonical run was touched.
- Added an explicit language-conflict control to the fixed-observation gate: negate the original drawer goal and then state the feasible cabinet goal. The 40-query confirmation remains exactly deterministic with zero hook fidelity error.
- The conflict condition is nearly equidistant between original and alternate at pooled level, but action alignment flips with trajectory step: at step 50 it is original-goal aligned, while at step 100 it is alternate-goal aligned. The VLM representation itself remains weakly alternate-aligned at both steps.
- Contradiction-dynamics analysis error: the first query-name parser treated `repeat_original` as an `original` group and constructed a nonexistent `repeat_alternate_goal` key. Tightened the suffix filter and reran; no raw data or report was changed by the failed attempt.
- Added and ran `scripts/analyze_phase2_contradiction.py`. At step 50, layer-15 expert alignment moves from `+0.0366` at the first denoising call to `-0.1907` at the last, and executed velocity is original-aligned throughout (about `-0.23` to `-0.27`). At step 100, expert alignment strengthens from `+0.1216` to `+0.2546` and executed velocity from `+0.2010` to `+0.3300` toward the alternate goal.
- Canonical expanded fixed-forward report: `reports/phase2_forward_gate/phase2_forward_gate_20260727T114152Z/`; raw exact tensors remain ignored under `local/phase2_forward_gate/phase2_forward_gate_20260727T114152Z/` (204 MB). No simulator rollout or immutable run was changed.
- Retrieved the revision-pinned official `lerobot/libero` data/meta Parquet only (`a1aaacb7f6cd6ee5fb43120f673cebb0cfea7dd4`): 1,693 episodes, 273,465 frames, about 22 MB locally, and zero video files or external model weights.
- Download execution issue: the initial four-worker shard retrieval stopped making progress at 90/382 files. Interrupted it without deleting partial data, changed the downloader default to one worker, resumed, and completed all requested data/meta files.
- Added `scripts/audit_training_trajectory_retrieval.py` and completed the designated v3 audit over 190,508 horizon-50 training windows, 16 fixed-forward proposals, and four archived rollout futures.
- Literal-copying control is negative: zero exact action-window matches; proposal same-task nearest standardized RMSE median `0.553` versus training self-nearest median `0.449`. State-conditioning worsens action similarity to `0.813`, and action-only other-task matches are generic low-motion tails with median state mismatch `1.707`.
- Same-seed plans are much more similar across success/failure episodes at the same landmark (`0.222`) than across landmarks (about `0.624`), while within-state proposal dispersion is `0.176`. Recorded a phase-locked policy-attractor hypothesis rather than an episodic memorisation claim.
- Added a runtime-local LIBERO config builder so future runs resolve the active package data and relocated read-only assets instead of inheriting stale archived absolute paths. Added `tests/test_runtime_assets.py`.
- Added `docs/phase2_discoveries_and_ersd.md`, which integrates the exact-forward, contradiction, executed/padding/null, simulator-state, and retrieval results into the ERSD/CPRD methodology and predeclares the smallest decisive Phase 3 tests.
- Documentation inspection issue: one broad read of the blind-spot audit and active records exceeded the tool output limit and was truncated. Replaced it with heading, section, and tail reads; no inference depended on omitted output.
- Planning-finalization patch error: the first task-plan patch omitted the list marker in its expected status context and did not apply. Re-read the exact section and applied a targeted patch; the failed attempt changed no file.
- Phase 2 final verification: 18/18 unit tests passed, all new Phase 2 scripts/modules compiled, and `git diff --check` passed.
- Final `bash scripts/resume_check.sh --full` passed: active-plan discovery, working Python/core imports, immutable canonical benchmark availability, and 18/18 targeted tests all succeeded.
- **Status:** complete
- Resume point: Phase 3 two-task branched CRD smoke gate, pending explicit approval for up to approximately 160 continuations. Do not download π0.5/GR00T weights or start the simulation expansion before that decision.

## Session: 2026-07-27 — Phase 3 branched CRD smoke gate

- **Status:** in progress
- User explicitly approved the predeclared Phase 3 simulation scope and asked for autonomous completion followed by a thorough professional-engineering review and corresponding fixes.
- Scope remains bounded to the SmolVLA two-task smoke ceiling of approximately 160 continuations. No π0.5/GR00T download, Phase 4 activation patching, or write under `archive/full_experiment/runs` is authorized or required.
- Success contract: a certified, deterministic state bank; a resumable branch ledger with common-random-number provenance and exact branch accounting; common multi-goal predicates; estimable `V`, `Q`, and `Q-V`; conditional hidden-state comparisons without group leakage; fixed-noise factor contrasts; compact derived evidence; and a post-run implementation audit with regression tests for every confirmed defect.
- Re-ran the project resume protocol and `bash scripts/resume_check.sh`; active plan, core imports, and canonical benchmark checks pass. Planning-session catch-up reported no unsynchronised context.
- Inspected the Phase 2 state and exact-query APIs before designing new code. The existing snapshot captures MuJoCo, controller/interpolator, gripper accumulator, robot buffers, observables, clocks, vector flags, Gym RNG, global NumPy RNG, object geometry, contacts, grasp state, and native goal predicates; it is suitable as the Phase 3 restoration primitive.
- Chose to reconstruct branch states by replaying the immutable benchmark's recorded executed actions from the same task/initial-state seed and validating replayed observations against the archive. This avoids rerunning completed policy rollouts while recovering the full runtime state that the old artifacts omitted.
- Inspection error: one metadata command used the system `python`, which lacks pandas. It made no changes; all project-data commands will use `local/lingbot-conda-env-archived/bin/python` as required by the resume guide.
- Benchmark feasibility audit: task 3 provides ten trajectories of 177–300 steps with six successes and four failures, while all task-4 trajectories succeed by steps 79–87. Task-4 step-100 snapshots therefore do not exist and must not be fabricated or obtained by ignoring termination.
- Tooling note: a `find | head` path inspection emitted a harmless broken-pipe message after `head` closed the stream. The needed path was found; future inspections use direct bounded paths rather than repeating that pipeline.
- Read both shared-scene BDDL contracts. The drawer task's exact native goal is `In(bowl, top_region)` and the cabinet task's is `On(bowl, top_side)`; this supports one explicit two-predicate evaluator and prevents language labels from being confused with task-native success flags.
- Audited the LeRobot environment wrapper. Initial-state selection is keyed by `episode_index/init_state_id`, while Gym's seed controls randomness separately; vector stepping uses same-step autoreset. Recorded both requirements in the Phase 3 branch contract.
- Found a more serious wrapper interaction before launching branches: the scalar LIBERO wrapper resets on success and the vector wrapper also uses same-step autoreset, after which the experiment loop performs another explicit reset. The benchmark's `initial_state_id=episode_index` field can therefore be wrong after a prior success. Paused branch implementation to add archive-based init-state resolution rather than propagating bad provenance.
- Confirmed the suspected double reset directly in the installed Gymnasium `SyncVectorEnv.step` source. The provenance issue is real at code level; the next check compares unaffected step-0 observations across the four nominally matched perturbation runs to measure its scientific impact.
- A first full camera comparison was too I/O-heavy for the execution wrapper and returned only partial output before the process ended. Changed the audit to read only the small step-0 `policy_state` member from each NPZ rather than repeating the expensive approach.
- The efficient archive audit confirms severe nominal-pair mismatch: exact step-0 policy state in clean versus paraphrase/blur/wrist is only `60/90`, `33/90`, and `14/90`. This changes the interpretation of the earlier perturbation study and will be added to the canonical experiment ledger; Phase 3 will use one captured snapshot per matched branch family rather than seed-based recreation.
- Verified the benchmark observation contract before implementing replay: saved cameras are `3x256x256` uint8, policy state is 8-D, robot joint state is 7-D, and preprocessing only converts raw HWC pixels to channel-first `[0,1]` before those arrays are recorded.
- Provenance inspection found that the benchmark was created from dirty commit `894c40a` through the former `experiments/...` path, whereas the relocated current runtime constructs 360-pixel environments. Phase 3 replay must reproduce the archived 256-pixel environment contract explicitly instead of inheriting the current helper's dimensions.
- Audited SmolVLA query APIs. Phase 3 will capture 80 unique first proposals once, reuse each stored chunk across its two matched continuation schedules, and generate only subsequent replans with explicit schedule seeds. This preserves the `Q` conditioning while avoiding 80 redundant exact forwards.
- Found a second preflight contract defect: Phase 2's saved cameras were recorded before LIBERO's required 180-degree image flip, but the fixed-forward script bypassed `env_preprocessor` and queried them unflipped. Paused Phase 3 implementation; the script and tests will be repaired and only the necessary corrected fixed-forward gate rerun before selecting latent sites.
- Added one reusable archived-camera orientation helper, applied it to both Phase 2 camera inputs, and added focused shape/orientation regression tests. Focused tests pass 5/5, the script compiles, and `git diff --check` is clean.
- Completed the corrected four-state fixed-forward gate as `phase2_forward_gate_20260727T123138Z`: 40 saved-observation queries, zero hook/repeat error, no simulator rollout, and no immutable artifact write. Ran the contradiction dynamics analysis on the corrected raw capture.
- The corrected input materially changes the science. The prior step-50/step-100 semantic-routing reversal disappears; contradiction expert/flow alignment is generally alternate-goal aligned at both landmarks, with only the final step-50 velocity weakly original-aligned (`-0.056`). Wrist-mean action RMSE becomes `0.958`, larger than alternate goal `0.472` and paraphrase `0.350`.
- The executable/null result survives and strengthens: controlled-factor hidden displacement is `97.85–98.64%` output-null, while proposal-noise displacement is `67.14%` padding-row and `74.98%` full-output-row. Corrected within-state action/expert coupling falls from the provisional `0.872` to `0.441`.
- Revisited the existing grouped-probe and retrieval utilities before designing Phase 3 analysis. Restricted the smoke analysis to direct paired estimates and low-dimensional, state-grouped validation rather than attempting an underpowered 720-D success probe on ten states.
- Added the Phase 3 core contract: ten explicit state specs, portable drawer/cabinet predicates, 80 unique first-query IDs, exactly 160 branch IDs, two common continuation schedules, deterministic seed derivation, low-dimensional query summaries, atomic JSON writes, and strict branch-ledger accounting.
- Added snapshot deserialization for resumable branches and focused tests for round-trip metadata, common-goal evaluation, branch uniqueness/ceiling, CRN seed schedules, historical reset indexing, action-summary validation, and ledger rejection. Focused Phase 3/state/orientation tests pass 10/10; compilation and whitespace checks pass.
- Storage-completeness patch issue: an initial assertion patch used stale test context and did not apply. Re-read the exact test and added the query `complete` assertion with a targeted patch; no file was changed by the failed attempt.
- Runner inspection confirmed the large Phase 3 patch landed in full and compiled. A follow-up metadata display requested a nonexistent `steps` column instead of the canonical `total_steps`; the read failed without changing data and was rerun with schema-aware column selection.
- Pre-run engineering audit found a fatal restore bug before GPU use: Zarr 2.18 groups do not expose `.parent`, but `_run_branch` attempted to read the source snapshot through that attribute. The runner now deserializes from the root state store and passes the immutable snapshot explicitly.
- Hardened Phase 3 persistence before launch: states and exact-forward queries are staged under local `.partial__*` Zarr keys and moved into place only after complete writes; persisted query identities and summary companions are checked; branch and query ledgers reject unexpected IDs; continuation noise hashes are recorded; and the contract now fingerprints the canonical source manifest/episode table, goal text/predicates, seeds, factors, image resolution, and activation sites.
- Expanded focused regression coverage for transactional query storage and the exact 80-core/160-total query ledger. Phase 3/capture/state/orientation tests pass 15/15; the runner and modules compile and `git diff --check` passes.
- Sandbox preflight could not communicate with the NVIDIA driver. This is the expected restricted-device boundary, not a project defect; the approved one-state/in-place smoke is being launched through the workstation execution boundary instead of retrying inside the sandbox.
- First in-place branch smoke stopped during the state certificate before any state, query, or branch was committed. `evaluate_common_goals` passed capitalized BDDL predicate names (`In`/`On`) directly to LIBERO, whose runtime registry asserted because it expects normalized predicate keys. The failed run is retained as resumable local evidence; the evaluator is being repaired and regression-tested before resuming it.
- Normalized only the predicate operator at the runtime boundary while preserving the canonical BDDL spelling in the frozen scientific contract. The fake evaluator now asserts the exact lower-case LIBERO API contract. Focused tests remain 15/15, compilation/whitespace checks pass, and inspection confirms the failed smoke committed no state/query/branch artifact.
- The resumed certificate reached repeated-action comparison and rejected harmless floating-point roundoff: MuJoCo differed by `1.91e-14`, non-image observations by at most `1.65e-16`, both cameras were bit-identical, and rewards/done/goals matched. The implementation incorrectly required every numeric observation to be exactly equal despite the predeclared tolerance-based contract. No state/query/branch was committed; explicit pixel/numeric/physics tolerances are being frozen and tested before a fresh contract run.
- Replaced the accidental exact-float test with a mixed certificate: cameras must remain bit-exact, other numeric observations must be within `1e-12`, and MuJoCo state within `1e-10`. These thresholds are now part of the contract and persisted certificate. A regression test accepts measured roundoff but rejects one-pixel or material numeric drift; focused tests pass 16/16 and compilation/whitespace checks pass.
- Passed the fresh-contract in-place smoke at `local/phase3_crd/phase3_crd_20260728T021125Z`: 2/160 branches, 1/160 exact queries, and two reconstructed source states committed transactionally with no partial groups or errors. Both states match archived step-0/landmark observations exactly; round trips are exact; repeated three-action certificates keep both cameras bit-identical and physics within `1.91e-14`.
- Persisted-payload audit confirms the two branches share exactly one query/chunk, use distinct predeclared continuation schedules, record the expected deterministic noise hashes, start with both common goals false, execute 150 steps, and terminate cleanly at the horizon. The smoke artifact is 7 MB; no immutable archive path was written.
- The full resume progressed into task 4, then stopped before committing `task04_ep001_step0050`: cameras, rewards, done, and goals were identical; MuJoCo drift was `6.36e-12`; a derived gripper-velocity observation drifted `2.35e-12`, narrowly exceeding the `1e-12` numeric cutoff. This reveals that the observation tolerance was still overfit to the first task. Completed atomic branches remain valid and will not be repeated; a narrowly audited monotonic contract amendment will relax only the numeric observation ceiling to `1e-10` while retaining bit-exact pixels and the `1e-10` physics ceiling.
- Added a guarded, explicit resume migration that accepts only a monotonic relaxation of the numeric-observation certificate field and rejects horizon or any other contract change. It preserves the old/new contract hashes, thresholds, timestamp, and reason in `contract_amendments`. The existing incomplete run is mechanically verified as eligible; 96 branches, 48 queries, six state groups, and zero partial groups remain intact. Focused tests pass 17/17.
- The migrated resume certified task-4 episode 1 but correctly stopped on episode 2: repeated three-action probes differed by `2.40e-08` in MuJoCo state and one intensity level in the wrist image, with gripper fields around `1e-10`. This is qualitatively beyond the prior serialization roundoff and may expose omitted contact-solver warm-start state. No episode-2 state/query/branch was committed; the restoration primitive is being audited rather than weakening the certificate again.
- Confirmed the omission in code: robosuite's current `MjSim.get_state()` serializes only time, `qpos`, and `qvel`; it excludes `act`, control/applied forces, mocap/user state, and especially `qacc_warmstart`, which MuJoCo uses to initialize the next contact solve. Extended snapshots to capture and restore every available full-physics/control field, with strict field/shape checks and fake-simulator regression coverage.
- Added a second narrowly guarded contract upgrade that permits only the addition of the declared full-simulator field list and records the old/new hashes and rationale. It cannot alter horizon, goals, seeds, or thresholds. The current incomplete run is mechanically eligible, and focused tests pass 18/18.
- The first full-simulator restore attempt stopped before its certificate because JSON serialization collapsed empty `mocap_pos` from shape `(0, 3)` to `(0,)`. No state/query/branch was committed. The simulator-field encoding is being corrected to preserve explicit shapes for zero-sized arrays before repeating the same two-branch verification.
- Simulator arrays now persist explicit shape plus values, with backward-compatible reading for older snapshots. Added a zero-sized `(0, 3)` mocap regression case; focused tests remain 18/18 and compilation/whitespace checks pass. The contract upgrade is recorded, while the ledger remains at 112 branches/56 queries with no partial artifacts.
- The two-branch contact-state verification now passes and advances the ledger to 114/160. The formerly failing `task04_ep002_step0050` matches the archive exactly and, after full simulator-field restore, repeated three-action probes have exactly zero MuJoCo, camera, and robot-observation difference (30 contacts at the source state). A no-checkpoint field ablation is next to identify which omitted field caused the old divergence.
- The first field-ablation report was written but deliberately failed its positive-control assertion: dropping only `qacc_warmstart` exactly reproduced the old `2.40e-08`/one-wrist-pixel divergence and every other single-field drop was exact, but the very first full restore into a newly constructed environment diverged substantially before later restores stabilized. The script had assumed a cold cross-instance restore was valid. This exposes a separate process-resume defect; the report will be regenerated with cold-start and hydrated controls separated.
- Phase 3 branch resume must not deserialize a snapshot into a fresh simulator and immediately execute policy actions. The runner will reconstruct every needed source episode once per process from its exact archived action sequence, revalidate archive fidelity/certificates, and use that new in-memory snapshot for all remaining branches. Persisted snapshots remain provenance evidence and query sources, not trusted cross-process branch roots.
- Regenerated `restore_field_ablation.json` with separate cold-start and hydrated controls. The cold cross-instance positive control diverges by `0.1262` in state and up to `191/255` pixels; after hydration, full restore is exact. Removing only `qacc_warmstart` reproduces the smaller `2.40e-08`/one-pixel within-instance divergence exactly; all nine other single-field removals remain exact.
- Refactored the branch runner so each source episode is reset, archive-action replayed, archive-validated, and certified once in every process before any missing branch uses it. Existing state artifacts are cross-checked against the new replay but never used as cross-process branch roots. New branch payloads carry the per-process reconstruction mode, state hash, archive fidelity, certificate, and persisted-state comparison.
- Added a guarded contract amendment for the per-process archive-replay source rule; it rejects any simultaneous change. Focused tests pass 19/19, scripts compile, and whitespace checks pass. A two-branch resumed smoke will verify the new payload and replay path before final completion.
- The resumed-source smoke passes at 116/160: the two new branch payloads share the same replayed state hash, exact archive/state-artifact comparisons, and a zero-difference three-action certificate.
- Deterministic replay audit of the first branch created across the old process boundary fails semantically. The legacy payload reported drawer success at step 134; replay from exact current-process archive reconstruction fails at the 150-step horizon, with different first-10/first-plan/final effects. The affected scope is exactly 30 task-3 branches: 14 remaining step-50 branches plus all 16 step-100 branches from the two states persisted by the initial smoke. Later source states were reconstructed within their branch-producing process.
- The repair will preserve all 30 legacy JSON payloads under a superseded-evidence directory with old hashes, remove them from the active ledger transactionally, and regenerate only those IDs from one current-process replay. Existing exact proposal queries remain valid and will be reused.
- Implemented the scoped refresh as an interruption-safe transaction: the manifest records intent first, each legacy payload is moved with its SHA-256 into `superseded_branches/legacy_cross_instance_restore`, missing active IDs are naturally resumable, and completion requires all 30 old/new files before recording new hashes. Added pure regression tests for the 30-ID scope, backup/resume/finalize behavior, and semantic replay comparison. Focused tests pass 21/21.
- Completed the repaired Phase 3 execution: exactly 160 active branches and 160 exact-forward query artifacts, ten complete states, no partial groups, 80 proposal chunks each referenced by exactly two branches, and deterministic continuation seed/noise hashes. The 30 superseded payloads and both old/new hash maps are preserved; all 30 semantic payloads changed and 12/30 success labels flipped after correct reconstruction.
- Initial exploratory summary error: a quick first-plan goal count indexed the effect dictionary at the goal name instead of its nested `goals` field and stopped after printing earlier aggregate tables. No artifact changed; the formal analyzer will use schema-validated extraction rather than repeating the ad hoc expression.
- Second exploratory summary error: pandas preserved branch success as boolean in a schedule pivot, so direct subtraction raised a type error after the proposal-variation table had printed. No artifact changed; the formal analyzer will cast outcomes explicitly before paired arithmetic.
- Dependency probe found that `statsmodels` is not installed in the archived working environment. No installation is needed: the analysis will use NumPy/SciPy/scikit-learn, empirical-Bayes binomial shrinkage, and explicit grouped cross-validation rather than adding a dependency mid-study.
- Formal analyzer validation error: the query-reference `Series` was converted to a set of counts rather than a set of index IDs, so the valid 80×2 ledger was rejected. The analyzer stopped before writing any statistical table; the validator is being corrected to compare `value_counts().index` explicitly.
- After the validator fix, the analyzer found a scientific invariant violation and stopped before writing statistics: the two continuations for `task03_ep001_step0100__goal_drawer__proposal_202` have different first-plan effects despite sharing the exact state/query/chunk and diverging only after action 50. This proves that old within-process branches lacking solver-state/per-process reconstruction provenance can also carry hidden simulator leakage.
- The trustworthy boundary is now explicit: 76 active payloads carry the new `source_reconstruction` certificate and 84 do not. All 84 uncertified legacy payloads will be hash-preserved and regenerated from per-process archive replay, producing a uniform 160/160 certified branch ledger. Queries/factor forwards remain valid and will not be repeated.
- Completed the provenance-wide repair: 160/160 active payloads now carry per-process archive reconstruction; all 80 paired first-plan effects are identical across continuation schedules. The 84 old payloads and old/new hashes are preserved; 66/84 semantic effects changed but no additional success label changed beyond the 12 flips already corrected by the first scoped refresh.
- Analyzer resume error: validation still assumed exactly one refresh transaction and rejected the now-correct two completed refresh records. It stopped before statistics; the check is being generalized to require that every recorded refresh is complete.
- Completed the formal Phase 3 analysis after generalizing refresh validation. The repaired active ledger passes all gates: 160 branches, 160 queries, ten states, 80 core queries referenced exactly twice, 80 factor queries, current-process source provenance on every branch, and zero paired first-plan mismatches or partial groups.
- Corrected outcome evidence is `73/160 = 0.45625` success. The descriptive variance decomposition is state-goal `80.79%`, proposal-within-state-goal `15.43%`, and continuation-within-proposal `3.78%`; only 3/80 continuation pairs disagree and 6/20 state-goal cells vary by proposal.
- Recoverability is cabinet-source cabinet/drawer `1.0/0.0` and drawer-source drawer/cabinet `0.45/0.375`. Added the descriptive veto–composition decomposition: cabinet-source language switching suppresses source completion by `1.0` but constructs drawer success at `0.0`; drawer-source suppression/constructive transfer is `0.45/0.375`.
- Conditional hidden-state tests are negative for the predeclared summaries. At `alpha=100`, `V` RMSE changes `0.386→0.389`, pre-execution `Q` `0.444→0.452`, effect-controlled `Q` `0.413→0.422`, and effect-controlled `L` `0.200→0.199`. The controlled-`Q` MSE improvement is `-0.0076` with source-episode bootstrap interval `[-0.0182,+0.0025]` and remains non-positive at alphas 10, 100, and 1000.
- ERSD analysis from exact Phase 3 queries yields median controlled-factor output-null energy `98.17%` versus proposal-noise output-null `21.35%`; proposal noise has median executed-row energy `8.18%` and padding-row energy `70.38%`. Pairwise action/expert distance correlates only `0.324/0.313` with absolute `Q` difference.
- Senior scientific review found that the source state bank is not progress balanced. All five cabinet states are step 50 with median bowl height `0.972`, 3/5 grasped, and a closed drawer; drawer states mix steps 50/100 with median height `0.898`, none grasped, and a displaced drawer in 4/5. Because the policy is reset and has no observation-history queue, the result is current-state/occupancy conditioned—not evidence of recurrent trajectory memory.
- Added `state_geometry_audit.csv`, the veto–composition summary, and an explicit raw-factor caveat: Phase 3's `contradiction` label is contrastive negation of the other goal followed by reaffirmation of the target, not an internally inconsistent instruction.
- Engineering review found and fixed three further issues without rerunning any branch: (1) paired-prefix equality is now enforced in the runner before the second branch is committed; (2) nested certificate comparison now fails closed on missing fields, shape changes, or unequal nonnumeric data and requires the full probe-action count; (3) the native/counter interval now resamples source episodes rather than treating repeated landmarks as independent. The drawer interval widened from `[-0.10,+0.25]` to `[-0.25,+0.1875]`.
- A first combined comparator patch used the wrong import context for `audit_phase3_restore_fields.py` and did not apply. Re-read the file and applied the change in a context-correct patch; no artifact changed in the failed attempt.
- Added `docs/phase3_engineering_review.md`, corrected stale Phase 2 orientation/reversal claims, updated the active plan/README/resume guide/causal programme/experiment ledger, and introduced occupancy-balanced CRD as the next proposed gate. Phase 4 and π0.5/GR00T scaling remain deferred because the current hidden-information gate is negative and the state-selection confound is unresolved.
- Final verification passes: 41/41 full tests, relevant script/module compilation, `git diff --check`, and `bash scripts/resume_check.sh --full`. The final integrity audit reports manifest `complete`, exactly 160 branches/160 queries/10 states, no partial groups, and exact matches for both source manifest and source episode-table hashes.
- Canonical compact Phase 3 evidence: `reports/phase3_crd/phase3_crd_20260728T021125Z/`. Raw evidence remains workstation-only at `local/phase3_crd/phase3_crd_20260728T021125Z/`. Nothing under `archive/full_experiment/runs` was modified, and no π0.5/GR00T checkpoint was downloaded.
- **Status:** complete
- Resume point: Phase 3b occupancy-balanced recoverability design, pending an explicit new simulation-scope decision. Do not repeat the completed Phase 3 matrix or proceed directly to causal patching with the current summary features.

## Session: 2026-07-28 — Next-step and portable publication audit

- **Status:** complete
- User requested the next recommended research step and cloud-accessible publication of all safe project work for use from another device.
- Reloaded the active plan with the planning-with-files workflow and ran `bash scripts/resume_check.sh`; the project records, working Python, imports, and immutable canonical benchmark checks pass.
- The first incomplete scientific phase remains occupancy-balanced Phase 3b. Before recommending execution, this session will audit the exact minimal design, Git scope, remote configuration, ignored/large-file boundaries, and available cloud-access path.
- Publication safety boundary: code, tests, documentation, planning records, and compact derived reports may be committed and pushed. Checkpoints, raw runs, activations, observations, videos, environments, credentials, and other workstation-only evidence must remain outside Git even though the user requested “everything.”
- Audited `origin` as the GitHub repository `Dz803/smolvla-hidden-state-analysis`, with local `main` initially equal to `origin/main`. The workspace contains about 1.8 MB of compact reports and no files over 10 MB in the publication scope; the secret-pattern filename scan found no matches. `archive/`, `local/`, `vendor/`, `logs/`, and `planning/` remain ignored. A sandboxed remote probe could not reach GitHub, so the final push will require network-enabled execution.
- Specified `docs/phase3b_methodology.md`. Stage A builds 32 policy-independent candidates across drawer, possession, locus, demonstration-support, and layout factors and certifies both goals before any VLA query. Stage B is a separately gated 16-state SmolVLA pilot capped at 128 branches; Stage C is a locked confirmation set capped at 256 branches and is not automatically authorised.
- The method makes constructive goal switching—not mere source-skill vetoing—the primary language-grounding contrast, and tests support/occupancy effects at matched oracle cost. Directional hidden features are fold-local and must beat complete observable/proposal-effect baselines on held state families before Phase 4 patching.
- Added a durable publication protocol to `AGENTS.md` and cross-device instructions in `docs/portable_results.md`: every verified Git-safe checkpoint is committed and pushed with a reported hash, while raw evidence requires a separately selected private object store and checksum manifest.
- One combined documentation patch referenced a progress-log line under the wrong file update and failed without changing any file. Reapplied it with explicit file sections and added the missing rule that every Phase 3b process must reconstruct and recertify its state rather than trust a cross-process snapshot.
- Verification after the design/documentation changes passes: 41/41 project tests, `bash scripts/resume_check.sh --full`, and `git diff --check` all succeed. No canonical rollout was rerun or modified.
- The first `git add -A` attempt failed because the managed sandbox mounts `.git/index` read-only. Retried the same scoped staging action with elevated Git permission authorised by the user's explicit upload request; no project content changed during the failed attempt.
- The first staged whitespace audit exposed three trailing-space lines in previously untracked Markdown artifacts. Removed the whitespace (using a list for the Phase 3 review metadata) before commit; no scientific content changed.
- Final staged-publication audit covers 141 Git-safe paths. `git diff --cached --check` passes, the staged secret-pattern scan returns no matches, and no staged path uses a prohibited local/raw prefix or checkpoint, video, Zarr, or Parquet suffix.
- Created commit `7d267ac6e538f2ab27262612e028e2a94bb5c37e` (`Complete causal validation through Phase 3`), pushed `main` to the configured GitHub origin, and verified that `refs/heads/main` resolves to the same hash. Compact results are now available cross-device; raw evidence remains deliberately workstation-only.

## Session: 2026-07-28 — Phase 3b Stage A affordance-lattice implementation

- **Status:** in progress
- User explicitly authorised Stage A while asking that the established resume, safety, testing, logging, and cloud-publication workflow remain in force.
- Reloaded the active `causal_validation` plan with the planning-with-files workflow. The first incomplete work is the policy-independent affordance lattice and oracle/certificate gate; Stage B/C policy branching, Phase 4 patching, and π0.5/GR00T downloads remain outside this scope.
- Stage A success contract: executable typed lattice specification; deterministic, policy-independent construction or replay in each hydrated process; both-goal feasibility certificates; computational-state probe certificates including full simulator fields; pre-policy balance/support reports; tests and compact documentation; no policy checkpoint load, no canonical rollout rerun, and no write under `archive/full_experiment/runs`.
- Initial record review reconfirms the two implementation-critical hazards: state selection must be locked before VLA outcomes, and persisted MuJoCo snapshots cannot be trusted across fresh processes without deterministic reconstruction/hydration plus the repeated-action certificate.
- Findings review adds two Stage A constraints: support distance must jointly condition robot state, object geometry, event, and motion rather than action-only nearest neighbours; and the saved interface must be environment/effect based so later model adapters do not compare incompatible raw action chunks or latent dimensions.
- The existing Phase 2/3 state contract already records object/site/joint geometry, contacts, bilateral grasp status, both portable goal predicates, controller/gripper/runtime state, RNG, and full simulator fields. Stage A should extend this tested primitive with typed construction provenance and oracle evidence rather than create a parallel snapshot format.
- Completed the full 301-line findings reload. Stage A will treat the two LIBERO predicates exactly as defined (`In(bowl, top_region)` and `On(bowl, top_side)`), require both false at every root, preserve 256×256 rendering provenance, and fail closed on autoreset or incomplete certificate fields.
- Reloaded the locked Stage A methodology and resume guide. The candidate space is exactly 32 cells (`drawer x possession x locus x support x layout`); the implementation must lock selection before policy outcomes, record both oracle costs, and keep support-pair oracle-cost mismatch at or below 10% unless explicitly failed/documented.
- Completed the experiment-ledger reload. No Stage A artifact exists yet, so there is no resumable construction run to continue and no completed state candidate that may be silently regenerated or overwritten.
- `bash scripts/resume_check.sh --full` passes at Git head `9a13a21`: active-plan records, working Python/imports, immutable canonical benchmark availability, and 41/41 tests are healthy before Stage A changes.
- API inventory confirms there is no existing Stage A module. Reusable boundaries are `LiberoStateSnapshot` capture/restore/round-trip in `libero_state.py`, portable predicate and certificate/accounting helpers in `phase3_crd.py`, and current-process reconstruction/probe/effect utilities in `run_phase3_crd.py`. `rg` is unavailable, so bounded `grep`/`find` inspections are being used.
- Full `libero_state.py` review confirms direct reuse is safe: snapshots include all ten declared MuJoCo runtime arrays (including `qacc_warmstart`), controller/interpolator/gripper/buffer/observable/vector/RNG state, geometry, contacts, grasp, and predicates. Stage A needs construction/oracle layers around this object, not changes to its scientific semantics.
- Full `phase3_crd.py` review identifies reusable fail-closed primitives: normalized portable goal evaluation, nested schema-aware observation comparison, fixed certificate tolerances, stable IDs/seeds, and atomic JSON writes. Stage A can stay policy-independent by importing only these environment/state helpers and must not import action-capture code on its execution path.
- Runner/state-gate review confirms Stage A should step the scalar control environment directly to avoid vector same-step autoreset, then use the existing repeated-action comparison. The current `_branch_certificate` is script-local, so a small reusable policy-free certificate utility is preferable to importing the Phase 3 runner (which loads policy-specific dependencies).
- Existing tests cover snapshot immutability, runtime-state restoration, portable predicates, exact pixel/numeric tolerances, schema changes, and Phase 3 accounting, but not constructed-state validity, construction IDs, oracle plans, collision checks, balance, or selection locking. These become the focused Stage A test surface.
- Local LIBERO inspection finds direct object/joint setters in the installed runtime and the exact two shared-scene BDDL files. This makes deterministic state construction feasible without a policy checkpoint, but grasped roots still require a physically valid contact-producing script rather than merely teleporting the bowl and labelling it grasped.
- BDDL/runtime review confirms both tasks share identical fixtures and initial regions; the drawer and cabinet targets are site objects on `wooden_cabinet_1`. LIBERO exposes site-aware containment/ontop tests and articulation setters, so the constructor can use exact site geometry and declared cabinet joint ranges instead of hand-coded world coordinates.
- The installed `WoodenCabinetObject` declares its open range as approximately `[-0.16,-0.14]`; closed ranges and per-drawer joint/site mappings will be read from the live object. Runtime asset/config discovery is already centralized in `runtime.py` and can be reused without loading model weights.
- The external asset tree contains the full articulated cabinet and bowl XMLs locally; no download is needed. Construction can therefore derive handle/target sites and collision geometry from the live compiled model, avoiding brittle constants tied to one fixture placement.
- Asset inspection confirms `top_level` is a slide joint with physical range `[-0.16, 0.01]`; `top_region` moves with that drawer, while `top_side` is fixed to the cabinet base. This validates drawer aperture as an independently manipulable factor and warns that drawer-target coordinates must be recomputed after each aperture setting.
- A five-step live action calibration shows Cartesian action dimensions 0/1/2 move the end effector by roughly `+4.4/+5.4/+5.4 cm`, rotations are coupled as expected, and gripper command `+1` closes the fingers. This is sufficient to prototype a deterministic feedback servo using only live geometry and the environment action API.
- The first policy-free grasp prototype approached directly above the bowl centre, closed for 15 steps, and lifted, but produced no grasp and left the bowl stationary. No artifact was promoted. Successful Phase 3 grasped states place the end effector about `+0.035–0.040 m` in world `y` and `+0.025–0.030 m` in `z` from the bowl centre, so the next probe will use that measured contact-frame offset rather than treating the bowl centre as the gripper centre.
- The first corrected-offset diagnostic stopped before taking any action because its temporary introspection helper requested body `akita_black_bowl_1`, while the compiled MuJoCo body is `akita_black_bowl_1_main`. This was an exploratory helper error only; no candidate or scientific artifact was written. The helper will use the compiled body name and repeat the same predeclared offset probe.
- The next diagnostic also stopped before taking action: the temporary summary referenced a nonexistent `snapshot.predicates` attribute instead of the snapshot's `goal_predicates`/portable goal evaluator. No candidate was written. The probe summary is being switched to `evaluate_common_goals`, the same portable evaluator already locked for Stage A.
- A third pre-action summary-only error requested `PandaGripper.current_qpos`, which this robosuite version does not expose. Because grasp status is already evaluated by LIBERO's bilateral contact check, the redundant display field is removed rather than adding another version-dependent API. No simulator action or candidate write occurred.
- The corrected offset probe now executes successfully: it reaches the demonstrated contact frame within `2.5 mm` and lifts the bowl by about `11.4 cm`. Nevertheless, LIBERO's strict bilateral `_check_grasp` remains false, so this is not yet an admissible `grasped` root. The close phase shifted the bowl about `9 mm` away in world `y`, leaving a final end-effector offset near `+44 mm`; the next calibration will reduce the commanded `y` offset and inspect explicit finger–bowl contact pairs.
- Orientation, not translation alone, was the missing grasp variable. A geometry-feedback servo that approaches at end-effector offset `[0,+0.040,+0.020] m` while converging to a fixed downward/yawed grasp frame produces contacts on both Panda finger pads; after a `0.12 m` lift, LIBERO reports `akita_black_bowl_1` strictly grasped and both goal predicates remain false. This policy-free primitive is now admissible for construction testing.
- The first cabinet oracle prototype reaches `On(bowl, wooden_cabinet_1_top_side)=true`, with drawer false, after release and retreat. The initial long, saturated transport temporarily lost bilateral contact before placement, so the final implementation must use rate-limited waypoints and verify possession at every carry segment; final-predicate success alone will not hide an unstable transport.
- Three hand-designed drawer-handle probes did not open the drawer: frontal closure contacted only one finger and slipped; top-down closure was blocked by the cabinet top; and a 90-degree wrist turn proved unreachable at the handle pose. No retained state or oracle artifact was written. This rules out accepting an unverified geometric handle target and motivates using the locally cached LIBERO demonstration controller trace as the policy-independent reference primitive, with exact initial-layout matching and no evaluated VLA query.
- The cached task-12 demonstration episode 382 opens the top drawer at action 52 and completes `In(bowl, top_region)` at action 157 from its closest live initialization. More importantly, the same fixed human-demonstration trace also completes the drawer goal from proposed layout replicates 0 and 1 (actions 165 and 163). This establishes a policy-independent action-space oracle reference for both layouts without loading or evaluating SmolVLA.
- A first hybrid drawer-oracle probe (demonstration opening prefix followed by geometry-feedback grasp/place) exited without a Python traceback before emitting its final summary, near the 300-step environment horizon. No artifact was written. The diagnostic environment will use a 1,000-step horizon and emit phase checkpoints so terminal-horizon effects can be separated from control failure.
- With the horizon raised to 1,000, the hybrid probe passes drawer opening, strict bowl grasp/lift, and rate-limited transport, then the process terminates while commanding the bowl centre to the drawer site's geometric centre. This localizes the failure to an over-deep contact target rather than the horizon or transport. The placement primitive will release from a `+0.06 m` bowl-height offset and let the bowl settle into the open region instead of forcing the gripper into the drawer volume.
- The subsequent hybrid trace revealed the actual upstream failure before any drawer descent: after opening, the custom IK servo did not reacquire the bowl, so its computed `grasp_offset` was invalid and the later target left the workspace. The final runner will fail immediately on each possession invariant and use the already successful full human-demonstration continuation for feasibility, rather than chaining an uncertified custom grasp after the drawer interaction.
- Cached task-18 demonstration episode 444 completes `On(bowl, top_side)` at action 83 on layout 0 and action 82 on layout 1 when replayed in the shared task-3 scene; task-18 episode 398 does not generalize to layout 0. The locked oracle references are therefore drawer episode 382 and cabinet episode 444, each action-hashed and evaluated through both portable predicates.
- Production assumptions are now explicit: layout replicates are LIBERO init IDs 0/1; Stage A uses the shared task-3 scene at 256×256 and a 1,000-step non-autoreset horizon; the cached human demonstration traces are fixed policy-independent oracles, not evaluated-policy data; support pairs are generated by rotating a scripted waypoint around the line joining the two live goal sites, which preserves Euclidean remaining distance to both goals; and actual active/path costs are reported alongside a predeclared padded oracle-step budget.
- Implemented the pure Stage A contract layer: a typed exact 32-cell factorial, deterministic IDs/family/support-pair IDs, layout mapping, pre-policy selection-lock hash, axis-rotation support construction, full-runtime snapshot hashing, and a fail-closed aggregate gate for policy isolation, root predicates, certificates, both oracles, goal-distance matching, and the 10% oracle-cost limit.
- Added six focused tests covering exact factorial/ID accounting, invalid levels, selection-lock tampering, dual-goal distance preservation, inclusion of `qacc_warmstart` in state identity, and record-gate failures for policy leakage, oracle failure, and support-cost imbalance. All six pass.
- The first production smoke constructed and certified its root but failed the drawer oracle cleanly before any candidate/state promotion. The fixed demo trace succeeds from a fresh reset, yet failed after Cartesian re-homing from the constructed root even though end-effector error was only `6.1 mm`; the redundant Panda joint/null-space and controller history were not reset to the live layout's initial computational state. The failed run remains raw-only at `local/phase3b_stage_a/phase3b_stage_a_20260728T065529Z/` with a locked manifest, 0/32 candidates, and a full error record.
- The corrective contract will target each layout's live reset end-effector orientation/position rather than the demonstration file's approximate initial observation, extend the fixed home-settling budget, and record live initial robot joints for diagnostics. Because this changes code/config hashes, it will start a new run rather than mutating or resuming the failed contract.
- The revised smoke stopped before oracle execution because the new initial-robot fields were accidentally passed into `_validate_root` instead of the `ConstructedCandidate` return object. This was a Python wiring error, not a scientific failure; the fail-closed runner wrote 0/32 and no state. The call/return mapping is corrected, and the changed source hash requires another fresh run rather than resuming the failed contract.
- The next smoke demonstrates that live-layout re-homing solved the oracle problem: the drawer goal first becomes true at demo frame 165 and remains true at the end, with only `0.0152 rad` maximum joint error from the reset posture. The runner nevertheless rejected it because LIBERO correctly returns `done=True` after task success for the final 12 actions. Root non-terminality remains mandatory, but oracle success termination is valid; the oracle will now stop at first target success, reject only a pre-goal terminal, and report executed steps separately from its fixed predeclared budget.
- The corrected production smoke passes and retains the first candidate (`closed/on_table/drawer_side/demonstration_near/layout A`) in run `phase3b_stage_a_20260728T070704Z`. Both roots are false; stability drift is zero for the bowl; there are no deep or robot–fixture contacts; the repeated three-action certificate is exactly zero in MuJoCo, both cameras, and every robot observation; drawer/cabinet oracles succeed at demo frames 165/83 with fixed budgets 377/296.
- A separate fresh Python process reconstructed that candidate from the locked script and layout, reproduced full state hash `46b8fa7d…a0b20` exactly, and repeated the exact zero-difference certificate. This validates the intended cross-process recipe/hash audit without trusting the persisted Zarr snapshot as a branch root.
- The paired `+90°` transverse construction failed before certification: its planned point behind the closed cabinet left the end effector `69.6 mm` from target after the full 180-step servo budget. No low-support state was written. Because rotation about the two-goal axis preserves both goal distances at any angle, Stage A will use a pre-policy `45°` transverse rotation (`~0.171 m` expected separation here) to retain a substantial controlled displacement inside the reachable workspace. This is a construction-feasibility calibration, so the revision advances to v2 and starts a new locked run; the successful v1 smoke remains preserved as diagnostic evidence.
- Post-revision verification passes: all six focused Stage A tests, compilation of the two modules and runner, and `git diff --check`. A later diagnostic file listing confirmed that `rg` is not installed in this shell, consistent with the earlier API audit; bounded `find`/parsed JSON inspection remains the fallback and no artifact was affected.
- The v2 matched causal smoke passes both members in raw run `local/phase3b_stage_a/phase3b_stage_a_20260728T071427Z/`: `closed/on_table/drawer_side/layout A` at `demonstration_near` and `transverse_low_support`. Both candidates are policy-free, nonterminal, have both portable goals false, preserve the untouched bowl to numerical precision, have no deep or robot-fixture contacts, pass the exact three-action certificate, and retain both physical goal oracles.
- The planned support points are separated by `0.170942 m` while preserving distance to both live goal sites exactly (drawer `0.2345671880 m`, cabinet `0.2261538169 m`). Controller realization leaves drawer distance `0.236664` versus `0.234257 m` and cabinet distance `0.227054` versus `0.224769 m` across the pair: pairwise mismatches of about `1.02%` and `1.01%`, safely inside the locked `10%` gate.
- Oracle budgets are exactly invariant across the support pair (`377` drawer, `296` cabinet). Executed counts differ by at most one step for each goal (`366/366` drawer and `284/283` cabinet), so neither feasibility nor padded cost balance explains the support contrast. The next smoke expands to grasped, open-drawer, cabinet-side, and layout-B extremes before committing the full 32-cell construction cost.
- The first highest-risk smoke (`open/grasped/cabinet_side/transverse/layout B`) passed construction far enough to enter its oracle, then failed closed during the generic grasped-root set-down transport: the controller lost bilateral bowl possession before reaching the table. No candidate record or Zarr state was promoted; the raw error is preserved under the same v2 run. This is an oracle-path implementation defect, not evidence that the root is invalid, and possession checks will remain strict while the failed carry segment is diagnosed and repaired.
- Deterministic diagnosis reproduced the failure exactly. The old `setdown_safe` leg exhausted all 80 actions while still `0.1216 m` from its target, but the oracle failed to inspect the phase's `pass` flag and continued into `setdown_descend`; that second leg then lost bilateral grasp. A generic high-clearance staged path retained the bowl initially but could not descend in the drawer-demo grasp posture, ruling out a simple budget-only patch.
- The robust recovery is constructor-relative: retrace the already successful root path to the constructor's lifted post-grasp waypoint, descend to its post-grasp waypoint, align over the reset bowl location, then descend and release. On the extreme layout-B root, the four calibrated legs finish within `7.5`, `8.9`, `11.4`, and `10.9 mm`, respectively, and strict bilateral grasp remains true after every leg. This supports a narrow v3 fix: persist typed recovery waypoints from construction, use fixed rate-limited return/table budgets, and fail immediately on either servo error or possession loss at each segment.
- Implemented the v3 recovery path and fail-closed per-leg validation; seven focused tests, compilation, and `git diff --check` pass. The production extreme-cell rerun advanced past recovery into home, then the raw LIBERO task terminated and rejected the next action. Inspection confirms the direct scalar control path bypasses LeRobot's wrapper horizon and uses LIBERO's native `horizon=1000`, while Stage A had configured only the wrapper to 1,200. Root timestep 400 plus fixed recovery brought the oracle close to the hidden native limit. No candidate/state was promoted in raw v3 run `phase3b_stage_a_20260728T073816Z`.
- The horizon mismatch is an execution-contract defect caused by deliberately stepping the scalar non-autoreset environment. The next revision will set and verify the underlying problem environment's horizon from the Stage A config and preflight the maximum root-plus-recovery-plus-home-plus-demo budget. Termination will continue to fail closed; it will not be masked or reinterpreted as oracle success.
- v4 propagates the configured 1,600-step horizon to the underlying LIBERO problem environment and locks a preflight minimum of 1,348 steps. Focused tests and compilation pass. On the repeated extreme cell, both recovery sequences pass all four carry tolerances, release, retreat, and home without termination; the drawer oracle succeeds, but the cabinet demonstration exhausts all 96 frames without either goal becoming true. The failed cabinet record has zero premature termination and zero wrong-goal events, so the remaining mismatch is physical state conditioning (open drawer and/or post-setdown reset fidelity), not horizon or lost possession. No candidate was promoted in v4 raw run `phase3b_stage_a_20260728T074355Z`.
- A controlled factor-isolation probe physically opened the drawer with frames 0–52 of drawer demo 382 while leaving the bowl unchanged to numerical precision, then homed the robot. Cabinet demo 444 failed in both layouts from these exact open-drawer baselines, proving the aperture intervention—not post-setdown noise—invalidates that proposal.
- A bounded, policy-free replay scan restored the same open-drawer/homed snapshot before each cached task-18 proposal. Thirty-one earlier length-ordered cabinet demonstrations failed the two-layout gate (episode 638 succeeded only in layout B); episode 474 is the first to succeed in both layouts, at frames 80/81, with action hash `5d93ae0b…f07150c`. This is direct empirical evidence for the planned decomposition of state feasibility versus proposal-specific luck: a failed successful-demo replay is not a failed affordance oracle.
- The robust episode-474 cabinet trace is locked pre-policy in construction revision v5. Its selection used only physical goal predicates under the two Stage A layouts and no SmolVLA output. The extreme production cell will be rerun from a fresh v5 contract; all v1–v4 diagnostics remain preserved raw-only.
- The v5 extreme cell (`open/grasped/cabinet_side/transverse/layout B`) passes and is retained in raw run `phase3b_stage_a_20260728T080705Z`. The root has both goals false, strict stable possession, no deep or robot-fixture contacts, and an exact three-action certificate. Drawer/cabinet oracles succeed at frames 175/80 with fixed budgets 947/873; all four recovery legs, release, retreat, and home pass their tolerances.
- A fresh Python process reconstructs the v5 extreme root to the exact full-state hash `30bd35a0…284c80` and reproduces zero difference in MuJoCo state, both 256×256 cameras, all robot observations, rewards, done values, and portable goal predicates. This closes the highest-risk construction/recovery/horizon/proposal smoke before expanding the lattice.
- Pre-expansion review found that v5's `demonstration_near` factor was still only a geometric construction label; it did not yet implement the methodology's required joint robot/object/event/motion nearest-demonstration measurement. Expanding 32 cells before fixing this would risk interpreting a label as empirical occupancy support.
- The first combined runner patch for the support bank failed cleanly because its README context string did not exactly match the file; no partial change was applied. Reapplied the import, contract, compact-report, audit, and execution changes in bounded context-specific patches.
- Implemented v6 joint support auditing against a policy-free bank built by replaying the two locked training demonstrations in both Stage A layouts. The metric jointly includes end-effector position/orientation, seven robot joints, bowl position, drawer joint, grasp-relative pose, distances to both goals, end-effector/bowl motion, action motion, and categorical layout/aperture/possession/locus/motion events. It reports both unrestricted and exact-event nearest references, the bank hash/coverage, and pairwise empirical direction without declaring direction by construction.
- Added a regression test proving that identical actions cannot defeat bowl-geometry, drawer-state, and event mismatches in the joint metric. All eight focused tests, relevant compilation, and `git diff --check` pass after the v6 implementation. The next gate builds the reference bank live and runs a small final-contract support pair before any full lattice expansion.
- The v6 live bank covers 503 deterministic reference states with hash `d17aa05f…023f26`; all four demo/layout coverage cells reach their intended goals. The first final-contract support pair passes, and the empirical joint distance increases from `0.40794` near to `0.41879` transverse while both candidates retain nine exact event-matched references. Planned goal distances are identical and both dual oracles pass. A fresh process exactly reproduces the state hash, zero-difference certificate, bank hash, and complete support measurement; the full suite passes 49/49 tests.
- A deeper pre-expansion scientific review found a remaining balance defect: the aggregate gate compared only padded oracle budgets, which are identical by construction and can conceal different active recovery work. For on-table roots, the two-centre rotation also preserved end-effector distance to the goal sites, not the unchanged bowl's task distance or the end-effector's distance to its oracle home waypoint. Earlier near/low diagnostics already show home active-step differences (`56` versus `19`) despite equal padded cost.
- Full expansion is paused before any additional v6 candidates. The corrective design is a three-centre geometric reflection: reflect the near controlled point across the plane through the drawer goal, cabinet goal, and the possession-specific recovery centre. This preserves distances to both goals and to the actual oracle recovery waypoint exactly; the final gate will additionally compare unpadded active steps, end-effector path length, and control effort rather than accepting padded budget equality alone.
- The full reflection is not universally executable: closed/on-table/drawer-side reproduces the previously unreachable point behind the cabinet, and some open/grasped reflections fall below the admissible workspace. The corrected construction projects the scripted waypoint onto the three-centre plane and places the pair symmetrically `±0.06 m` along its normal. All 16 non-support cells have both controlled and end-effector targets inside bounds; every pair has fixed `0.12 m` separation and exact equality to both goals and the recovery centre.
- The first v7 symmetric pair passes. Planned goal/recovery mismatch is numerical zero; realized drawer/cabinet mismatch is `1.17%/2.35%`, realized recovery mismatch `4.64%`, active-step mismatch `5.26%`, and end-effector path mismatch `0.84%/1.05%` for drawer/cabinet. Joint support rises materially from `0.3438` to `0.5251`, and scripted-waypoint distance rises from `0.0989` to `0.2183 m`.
- Review of the new cost ledger found that `control_effort` summed the full seven-dimensional action, so the constant `±1` gripper command on every padded step dominated it and made it another partly padded metric. The final contract will add motion-only six-axis effort and gate that quantity. It will also define `transit_locus` as predeclared route/anchor provenance while separately reporting the physically nearest fixture anchor; treating an inferred nearest-anchor label as the factor itself would silently change the factor semantics under the transverse intervention.
- v8 implements six-axis motion-only effort and locked route/anchor locus semantics. The small pair passes again with motion-effort mismatch `0.72%/1.14%` for drawer/cabinet, while joint support remains `0.3438→0.5251`; the nearest physical anchor switches for the opposite-side state and is now explicitly reported rather than silently changing the locked factor.
- The v8 high-risk `open/grasped/cabinet_side/near/layout B` root constructs successfully but its direct oracle return to the lifted recovery waypoint stalls `0.1825 m` away after all 220 actions. No candidate/state was promoted. This reveals direction-dependent Cartesian-controller/null-space hysteresis: equal Euclidean recovery distance and a successful forward path do not guarantee that a fresh feedback servo will retrace it in reverse.
- The next recovery revision will retain the proven root-servo actions only in process memory, record their hash/count as provenance, replay their six relative control axes in reverse with strict grasp checks, then apply a short target correction before the existing descent/release path. This uses the actually executed construction route rather than assuming path reversibility from endpoint geometry; raw action sequences will not enter the Git-safe compact report.
- v9 implements that route-aware recovery: construction retains the executed root actions only in memory, the durable record carries only their count/hash, and the oracle replays the six motion axes in reverse while keeping the gripper closed before a bounded endpoint correction. A new regression test initially used nested `pytest.approx`, which pytest does not support; replacing it with `numpy.testing.assert_allclose` fixed the test without changing production code, and all ten focused tests pass.
- On the exact high-risk `open/grasped/cabinet_side/demonstration_near/layout B` root, the v9 reverse path preserves strict bilateral grasp at every step and returns within `8.91 mm` position and `0.034 rad` orientation error. The subsequent set-down, release, retreat, and home phases pass, but locked drawer proposal 382 then exhausts all 177 actions without either goal. No candidate was promoted in raw v9 run `phase3b_stage_a_20260728T091446Z`; the result separates a repaired physical recovery path from another proposal-specific replay failure.
- A bounded cached task-12 scan reconstructed that same root, repeated the exact recovery/normalization path, captured one common post-recovery baseline, and restored it before every proposal. The bowl returned within about `1.6 mm` of its initial table position, the drawer remained open, neither goal was true, and episodes 576, 694, 614, and 382 all failed without premature termination. Episode 526 is the first length-ordered proposal to satisfy the drawer predicate, at action frame 166, with action hash `8ebeecff…ea3fc`. Selection used only the physical predicate and no VLA output; cross-layout/support robustness remains to be checked before locking v10.
- v10 replaced the drawer proposal with episode 526 and passed ten focused tests, compilation, and `git diff --check`, but its first high-risk construction failed before oracle execution: the root servo ended `0.262 m` from its target. The cause is a scientific role-coupling bug rather than random simulation failure: the same `drawer` trace was being used both as the feasibility proposal and as the prefix that constructs the open-drawer factor, so changing the oracle silently changed the state lattice and robot posture.
- v11 separates four locked demonstration roles: episode 382 constructs drawer aperture, episode 474 constructs grasped roots, episode 526 is the drawer feasibility oracle, and episode 474 is the cabinet feasibility oracle. Only the two oracle traces populate the support-reference bank; the contract now records each role, purpose, task, action hash, and frame count. This prevents oracle proposal selection from adaptively redefining construction while retaining the already certified v9 root geometry.
- Under v11, the high-risk near member passes completely, but the matched three-centre low member fails identically both after the near candidate and in a fresh process, ending `82.7 mm` from its target. Half-separation calibrations at `0.05`, `0.04`, and `0.03 m` still miss by `71.5`, `68.9`, and `65.8 mm`; the weak response rules out pair width as the main cause.
- A staged high-clearance probe localizes the physical invalidity. It preserves grasp through vertical and lateral transit, but descent toward the low endpoint triggers the cabinet goal and loses the bowl while still `65.3 mm` away. Thus workspace-bound and exact-distance checks admitted a point whose approach crosses the target fixture; mathematical three-centre balance is not a collision- or goal-aware feasibility certificate.
- A fixed `20°` rotation of the scripted waypoint about the live drawer–cabinet goal axis is policy-free, keeps the near state exactly on the scripted waypoint, preserves both goal distances analytically, produces a `48.2 mm` pair, and reaches the isolated high-risk low root within `9.43 mm` while retaining strict grasp and keeping both goals false. v12 replaced the invalid three-centre production construction with this fixed goal-axis rotation and changed planned recovery matching from exact equality to the predeclared `10%` limit.
- The v12 production high-risk pair completes both candidates and both oracles. Actual work is well balanced: active servo mismatch is `3.99%`, path mismatch `2.31%/2.47%`, motion-only effort mismatch `2.28%/2.65%`, and executed-step mismatch at most `0.11%`; planned goal distances remain exact and realized goal mismatches are `7.18%/0.0035%` for drawer/cabinet.
- The same audit rejects two stronger interpretations. Realized recovery distance differs by `13.97%`, and joint replay-bank support reverses the geometric label (`0.8510` scripted versus `0.8281` rotated); neither candidate has an exact event-matched reference. The crossed `open + grasped + cabinet-side + stationary` event cell is absent from the two demonstration traces, exposing a positivity/overlap failure rather than a metric implementation error.
- v13 therefore leaves the support labels as pre-policy geometric provenance and treats measured support direction/coverage as evidence, not a forced pass condition. Planned recovery remains gated at `10%`; realized recovery distance becomes report-only because the methodology's actual balance estimand is directly gated by executed steps, active work, end-effector path, and six-axis effort. This avoids selecting a post-hoc `15%` distance threshold merely to admit the observed pair.
- The pre-expansion engineering review hardened the contract before the 32-cell spend: canonical hashes now reject non-finite values; relative-difference gates fail on NaN/Inf; every action tracks goal, terminal, grasp, and relative-pose histories; construction padding/stability phases are validated; robot–fixture contact matching covers Panda/finger/hand names; the repeated-action certificate requires exact full-runtime snapshot hashes; oracle normalization cannot transiently cross either goal; and normalized bowl position is bounded in full 3-D.
- Resume/publication hardening now restricts raw runs to `local/phase3b_stage_a`, hashes all local transitive scientific sources, validates stored contract/manifest/inventory identities, preserves timestamped error attempts, re-reads and rehashes persisted Zarr payloads, constructs every candidate in a fresh simulator separate from the support-bank simulator, validates each support pair before persisting its second member, writes compact reports transactionally, and reports exact-event support coverage and label-direction reversals explicitly.
- v14's stricter path audit rejected the first grasp frame because LIBERO's bilateral predicate dropped during the following lift. A trace audit found a 9-frame contact interval at frames 128–136, a one-frame gap, then a 30-frame interval at 138–167. Waiting for ten contacts (v15/v16) preserved grasp but moved the arm into an IK/null-space state that missed the root by `24.9 mm` and made reverse recovery miss by `167 mm`.
- Per-step temporal instrumentation resolves the ambiguity. At the original frame-128 handoff, the custom safe lift has exactly one contact-negative tick with only `16.2 mm` maximum end-effector–bowl relative-pose deviation; the following root transport has zero dropouts and `0.68 mm` relative-pose deviation. A predeclared temporal possession rule therefore permits at most one consecutive predicate dropout and at most `20 mm` relative-pose deviation while still requiring final bilateral grasp. This distinguishes solver/contact flicker from a physical drop/regrasp.
- v19 restores the reversible frame-128 handoff under that richer possession criterion. The hardened high-risk pair completes in isolated simulators and passes its immediate physical/work gate; measured support still reverses by `-0.022920`, which is retained as evidence. A third fresh process exactly matches state hash `56f6b564…e44840a`, construction and reverse-path provenance, root geometry, support bank/measurement, both cameras and all robot observations, and the complete post-probe runtime hash (`6d2ea6bb…4f3bf`).
- Optional `ruff` and `pyflakes` checks were attempted but those modules are not installed in the archived environment. No dependency was added; focused pytest, `py_compile`, and `git diff --check` remain the available static gates.
- v20 replaced a fixed support rotation with the largest deterministic positive goal-axis rotation at or below `20°` that keeps planned recovery mismatch at or below `8%` and pair separation at or above `15 mm`. The calibrated closed/on-table pair uses `4.890111°` and the high-risk open/grasped pair uses `19.239841°`; both pass the online work gate. Their measured joint-support changes are `-0.003612` and `-0.023402`, respectively, and the high-risk pair has no exact-event reference, so geometric support labels remain provenance rather than measured occupancy claims.
- Expanding v20 exposed a drawer-oracle coverage failure that the high-risk smoke did not: episode 526 succeeds from the normalized open/grasped hard root but exhausts all 178 actions without a goal from `closed/grasped/drawer-side/near/layout A`. Construction, full-state certification, normalization, reverse recovery, and homing all passed; no failed candidate was persisted.
- A deterministic proposal scan restored one byte-identical normalized closed/grasped baseline before each of all 36 cached task-12 demonstrations. Twelve proposals succeeded: episodes `382, 414, 462, 489, 494, 523, 560, 694, 701, 737, 738, 784`; episode 526 did not.
- Retesting only those 12 successes from the previously validated hard open/grasped baseline found exactly two shared proposals: episode 489 (goal frames `183/184` in closed/hard roots) and episode 523 (`193/192`). This directly measures proposal-specific luck under matched physical roots rather than treating a single replay failure as infeasibility.
- Locked episode 489 as the drawer feasibility proposal for construction revision v21 because it is successful in both predeclared stress roots and reaches the goal earlier than the other shared proposal. Construction traces remain unchanged and role-separated, so the physical lattice is not adaptively redefined. All v20 runs remain diagnostic raw evidence and the final lattice will start under a fresh v21 contract.
- The v21 support-reference gate falsified that two-root choice before any candidate was written: episode 489 does not reach the drawer goal from source layout B. The raw run `phase3b_stage_a_20260728T120901Z` remains at 0/32. This adds source layouts A/B to the proposal acceptance contexts and shows that stress-root coverage alone is not sufficient.
- The support-bank exception exposed a resume defect: it occurred before the candidate-level `try`, leaving the manifest `in_progress` without an error record. v22 now catches support-bank construction failures, writes a timestamped structured error, and marks the manifest failed with `failure_stage=support_reference_bank`.
- Episode 523, the other proposal in the two-stress-root intersection, passes both source-layout support replays and yields a 561-state joint reference bank with hash `a412f95c…3d4519`. It is now the first drawer proposal validated across all four pre-policy acceptance contexts: source layouts A/B plus normalized closed/grasped and open/grasped stress roots.
- The v22 smoke run `phase3b_stage_a_20260728T121202Z` passes both matched stress pairs (4/32 candidates). The closed/grasped pair has empirical support delta `-0.047847`; the hard open/grasped pair has delta `-0.018694`. Both reversals are retained as positivity evidence while all physical/work gates pass.
- A fresh process exactly reconstructs the hardest v22 transverse root: state hash `c28c56a4…b23b84af`, reference-bank hash, construction, recovery provenance, root geometry, and support measurement all match. The repeated three-action certificate has zero MuJoCo, camera, and robot-observation difference and matching post-probe runtime hash `7c15e5c8…6e26ce`.
- Stage A's offline and causal smoke gates now pass under one frozen v22 contract. The same run will expand from 4/32 to the remaining locked cells; completed candidates will be skipped and no earlier diagnostic contract will be resumed.
- v22 expanded safely to 14/32, then failed closed on `closed/grasped/cabinet-side/near/layout A`. The direct 180-step diagonal servo reached within `7.97 mm` but lost bilateral possession for nine consecutive steps with `47.2 mm` relative-pose deviation. This is a physical drop/regrasp signature, not the calibrated one-tick contact flicker; the failed run remains preserved as `phase3b_stage_a_20260728T121202Z`.
- An isolated route diagnostic reconstructed that exact root and tested clearance paths from one restored post-lift snapshot. A three-leg vertical/horizontal/descent route preserved possession at every step, kept both goals false, and held relative-pose drift below `0.4 mm`; the initial `60`-step lift budget was simply too short. A `150/80/50` v23 route passed the failed pair and the closed/grasped drawer-side pair.
- v23 then falsified the 80-step horizontal budget on the prior hard open/grasped/cabinet-side layout-B root: grasp, goals, and termination remained valid, but the intermediate waypoint ended `51.2 mm` short. A `120`-step transit reached within `12.15 mm`, exposing the distinction between intermediate waypoint precision and the locked `12 mm` final-root precision.
- Route calibration with a `20 mm` intermediate tolerance passes the hard root; increasing planned clearance to `40 mm` guarantees positive residual clearance despite that tolerance. Extending descent from `50` to `70` steps improves final hard-root error from `11.5` to `4.65 mm` rather than accepting a near-exhausted phase.
- v24 locks a universal grasped-root route of `150/120/70 = 340` actions, `40 mm` planned clearance, `20 mm` intermediate tolerance, `12 mm` final tolerance, normalized root timestep `540`, and native horizon `1750`. Config validation requires positive complete phase budgets summing to the reverse-recovery count and clearance strictly greater than intermediate tolerance. Focused tests pass 12/12.
- v24 then exposed a controller-state asymmetry during recovery: algebraically negating and reversing the successful Cartesian route did not return the robot to the root, ending about `216 mm` away. The failed run remains raw diagnostic evidence; no candidate was promoted and no balance threshold was relaxed.
- v25 replaced inverse-action replay with feedback control through the physical route waypoints. On the hard grasped root it returned within `8.2 mm`, preserved final bilateral possession, limited global relative-pose drift to `1.25 mm`, and had no sustained contact dropout. The same run then failed because the selected drawer proposal remained root/posture dependent rather than because recovery failed.
- An exhaustive policy-free replay scan from the exact v25 hard normalized root found only drawer episodes `414` and `526` successful. v26 showed the complementarity directly: episode 414 succeeds on the hard demonstration-near root but fails on its matched transverse root, whereas episode 526 succeeds on the transverse root. A single demonstration cannot certify feasibility uniformly across this lattice.
- v27 therefore separated the support-reference trace from a locked feasibility proposal bank, restored the identical root for every attempt, recorded failed attempts and selected proposal identity, and added fallback/pair-selection reporting. The source-layout support bank passes with `555` states and hash `456f9c96…c07f39`; focused tests pass 13/13.
- The v27 matched hard pair stopped at `1/32` in raw run `local/phase3b_stage_a/phase3b_stage_a_20260728T150543Z`. The near member passed with drawer episode 414 and cabinet episode 474. The low member exhausted the locked cabinet bank: episode 474 executed all `1,073` budgeted steps after valid normalization, stayed nonterminal, reached neither goal, and left the bowl only `7.60 mm` from its normalized position. This is a cabinet proposal-coverage failure, not evidence of physical infeasibility; the failed run and timestamped error are preserved unchanged.
- Stage A remains paused before expansion. The next action is an exact-root, policy-free cabinet proposal scan, followed by a fresh contract with the smallest pre-policy bank that covers the declared stress/source contexts. Completed diagnostic candidates will not be mixed across contracts.
- Added a resumable policy-free scan path that captures and certifies one post-normalization full-runtime snapshot, restores it before every proposal, and locks the episode inventory/hash. Focused tests increased to 14 and now reject proposal-bank attempts whose normalized state or normalization-action hashes differ.
- The exact failed v27 low root scan completed at `local/phase3b_stage_a/proposal_scans/cabinet_20260729T023255Z`: root and normalized-root repeated-action certificates are exact; all 46 cached task-18 demonstrations were replayed from normalized state hash `fcc91d98…52c5c`; only episode `638` succeeds, at frame 74, with action hash `a8d81987…234932`. No attempt reached the wrong goal or terminated prematurely.
- v28 freezes the minimal ordered cabinet proposal union `[474, 638]`. Episode 474 remains first to preserve its broad source/near coverage; episode 638 is an explicit fallback for the measured transverse proposal basin. The support-reference trace remains episode 474, so this proposal-bank amendment does not redefine the support metric or state construction.
- The v28 hard matched pair passes in run `local/phase3b_stage_a/phase3b_stage_a_20260729T023742Z`. The near root selects drawer/cabinet episodes `414/474`; the transverse root falls back to `526/638`. Despite both proposal switches, the largest selected-oracle path/effort mismatch is below `7.5%`, all locked `10%`/`20%` work gates pass, and measured joint support changes in the labelled direction by `+0.005062`.
- A fresh process exactly reconstructs the v28 transverse root with state hash `1183d736…a16c3e`, route provenance, root geometry, support measurement, and the 555-state support-bank hash. The repeated three-action certificate is exact for MuJoCo state, both cameras, all robot observations, rewards, done values, predicates, and the post-probe full-runtime hash. The run remains resumable at 2/32 and is cleared for the remaining Stage A cells under the same frozen contract.
- v28 expansion committed ten candidates and four additional matched support pairs, then failed closed on `closed/grasped/drawer-side/demonstration-near/layout A`. Drawer episodes 414 and 526 both exhausted their full traces from the identical normalized state hash `486e2ca4…150969` and normalization-action hash `23dc2c66…f9bc4`; neither reached a goal, crossed the wrong goal, or terminated. The ten completed candidates remain valid under v28 and will not be rerun; an exact-root drawer proposal scan is next.
- The first 36-proposal drawer scan preserved 33 attempts, then exposed a LIBERO termination-interface bug: `BDDLBaseDomain.step()` overwrites the returned horizon `done` value with task success even though robosuite's internal `self.done` remains true, so the next action raises `executing action in terminated episode`. The failed scan remains preserved; it is not treated as a proposal result.
- v29 makes `PolicyFreeController` expose `returned_done OR problem.done`, with a regression test for the hidden native-horizon case. The fresh complete scan `local/phase3b_stage_a/proposal_scans/drawer_20260729T030120Z` certifies the same root/normalized hashes and finds 9/36 successful drawer proposals: `694, 382, 737, 489, 784, 701, 494, 523, 738`. The three longest traces stop cleanly at the production horizon after 240 proposal steps rather than crashing.
- Senior design review rejected another one-at-a-time fallback amendment as adaptive and inefficient. v30 instead freezes every cached task-12/task-18 human demonstration (`36/46`) in length/episode order, evaluates the full bank from one shared normalized snapshot, reports proposal-success coverage, selects the minimum executed-step/path/effort success deterministically, and compares each support pair with one proposal successful on both members. Configured horizon 1850 exceeds the exact all-inventory preflight minimum 1781.
- The exhaustive-bank design changes the estimand explicitly: feasibility is `max_k Y(s,g,k)`, state oracle cost is the minimum successful cost, proposal-basin width is the success fraction over the locked inventory, and matched support cost uses a shared successful `k`. This prevents demonstration identity from being silently absorbed into physical difficulty and avoids choosing proposal fallbacks only after individual lattice failures.
- v30 quantified the hard near root before failing its intentionally strict overlap gate: drawer succeeds for only `2/36` proposals (episodes 526 and 414), while cabinet succeeds for only `1/46` (episode 474). The transverse member has no cabinet success-set overlap with the near member because its sole success is episode 638. This proves that a 19.2° matched physical perturbation can switch the entire cabinet trajectory basin.
- Senior review rejected using zero proposal overlap as an exclusion criterion: that would select away the very off-trajectory states Stage A was built to study. v31 gates balance on each state's deterministic minimum feasible cost, reports success-set intersection/Jaccard, and adds same-proposal cost diagnostics only when an intersection exists. A regression test now requires a disjoint support pair to remain valid and report a zero shared-success count.
- The v31 hard matched-pair gate passes in raw run `local/phase3b_stage_a/phase3b_stage_a_20260729T033540Z` (2/32). Drawer coverage is `2/36` near and `1/36` transverse with one shared success (Jaccard `0.5`); cabinet coverage is `1/46` on each root with no shared proposal (Jaccard `0`). Selected minimum-cost path/effort mismatch remains below `7.5%`, support changes by `+0.005062`, and all root/certificate gates pass.
- Pre-full-run engineering review advances the reporting-only contract to v32. The compact artifact now preserves the complete candidate-by-proposal matrix, proposal generality, factor-wise proposal-coverage contrasts, deterministic candidate/state inventory hashes, and per-file artifact hashes written before the manifest. A focused regression test proves that the full bank is evaluated after an early success and selects the minimum-cost success; 16/16 focused tests and compilation pass. No scientific construction or oracle execution path changed from the passed v31 pair.
- `bash scripts/resume_check.sh --full` passes all 57 repository tests immediately before the full spend. The frozen v32 contract is `dc86cbdd…64597`, with 36/46 drawer/cabinet proposals and configured/native-minimum horizons `1850/1781`.
- Launched the one fresh full run as `local/phase3b_stage_a/phase3b_stage_a_20260729T035043Z`; no diagnostic contract is resumed. The first complete four-state family (`closed/on-table/drawer-side`) passes both layout-specific support-pair gates at measured distance deltas `+0.001077/+0.002495`. The run remains in progress and has not loaded a VLA.
- At 12/32, all closed-drawer drawer-side roots pass, including the grasped family that the v28 two-proposal bank could not certify. The full bank finds 9/36 drawer and 18/46 cabinet successes on the formerly failing near root.
- The first decisive occupancy falsification is replicated across both closed/grasped/drawer-side layouts: measured joint-support distance changes by `-0.046797/-0.049123`, so the geometrically transverse state is *closer* to the narrow replay bank. On-table support changes are only `+0.000296` to `+0.002495`. Physical construction succeeds, but the label does not create a universal measured occupancy contrast.
- Engineering/scientific review makes the oracle mediator explicit. Demonstration coverage is `Y(N(s),g,k)`, where deterministic recovery/homing `N` removes possession/locus before replay and leaves candidate-specific full controller/simulator states. It composes a valid feasibility path from `s`, but proposal-set differences cannot by themselves be called direct trajectory sensitivity at root `s`.
- v32 stops fail-closed at 16/32 on the first open-drawer candidate, `open/on-table/drawer-side/near/layout A`. All 36 complete task-12 demonstrations fail from one shared certified normalized state (`1a197793…5e84a`) after 7,157 demonstration steps; none reaches the wrong goal or terminates early. The raw run `phase3b_stage_a_20260729T035043Z` and its 16 closed-drawer candidates remain unchanged.
- This is a structural phase mismatch, not evidence that the open state is physically infeasible: every proposal is a complete closed-drawer task trace, so each begins with a drawer-opening phase that is inappropriate once the factorial has already set the drawer open. The next bounded diagnostic will derive event-locked pre-grasp anchors/suffixes by one uniform rule, bridge to the matching phase without using proposal success, and test that phase-matched oracle on the failed state before any new full run.
### 2026-07-29 12:40 CST — First phase-anchor smoke falsifies grasp-tail replay

- Reconstructed the first v32 open-drawer failure state without a support bank and reproduced the full episode-382 drawer failure from its certified normalized root.
- Defined the first open-to-close gripper transition directly from the action trace (frame 97 for episode 382), replayed the same demonstration from the matching layout to capture the pre-close end-effector pose, restored the exact normalized state, and used a fixed 200-step open-gripper bridge before replaying only frames 97 onward.
- The bridge was safe and deterministic: final position/orientation errors were `0.006351 m`/`0.000335 rad`, bowl drift was exactly zero, the drawer stayed open at `-0.152878`, neither goal was crossed, and no terminal occurred.
- The 80-action grasp tail nevertheless failed to reach either goal and never terminated early. Therefore pose matching only at the gripper-close boundary is insufficient; the continuation needs the preceding handle-to-bowl phase (or a feedback reachability controller), not merely a tighter interpretation of the complete-trajectory bank.
- Next bounded diagnostic: anchor each drawer trace at its first verified drawer-threshold crossing and replay the entire post-opening continuation. Episode 382 is especially diagnostic because that event is also the open-state construction boundary.
### 2026-07-29 12:48 CST — Drawer-opening event is not directly bridge-reachable

- Tested the semantically stronger episode-382 anchor at the first frame where the reference drawer crosses the locked open threshold: event frame 52, drawer joint `-0.144078`, suffix starting at frame 53.
- From the same certified normalized open-drawer state, a direct 200-step Cartesian bridge stalled `0.107078 m` from the reference event pose with `0.02894 rad` orientation error. This was identical under `0.012 m` and `0.002 m` stopping tolerances because every step remained active.
- Both bridge attempts were otherwise safe: zero bowl drift, no grasp, no goal, no terminal, and the drawer remained open. The 124-frame suffix failed after the missed bridge endpoint, so this is not a valid test of the phase suffix itself.
- Next bounded test replaces the direct bridge with the already specified three-leg clearance route (lift, horizontal transit, descent) before replaying the same post-opening suffix.
### 2026-07-29 12:54 CST — Clearance bridge validates phase mismatch; threshold scan exposes source-state loss

- Replaced the failed direct bridge with the existing locked three-leg clearance geometry. On the same episode-382/open-drawer root, lift/transit/descent finished at `17.80/11.37/7.27 mm`, respectively, with zero bowl drift, no grasp, no goal, and no terminal during the bridge.
- Replaying episode 382 only after its drawer-opening event then reached the drawer goal at original frame 164. The corresponding complete replay still failed. This clean within-root contrast isolates the obsolete drawer-opening prefix as the failure source for this proposal; it is not a missing physical affordance.
- Added a transactional raw-only scanner at `scripts/scan_phase3b_drawer_phase_proposals.py`. Its first run, `local/phase3b_stage_a/drawer_phase_scans/drawer_phase_20260729T045351Z`, retained 2/36 attempts and failed closed when episode 614 never crossed the drawer threshold under layout A. Episode 694 passed the phase continuation; episode 576 did not, and both bridges passed.
- Audited the revision-pinned LeRobot data schema. It contains only 8-D robot state, 7-D action, and episode/task indices; it omits object/environment state and source initialization identity. Consequently, replaying a source demonstration in a target layout cannot universally recover its semantic phase.
- Next diagnostic uses a source-action-intrinsic rule: the first gripper-close transition minus a fixed 50-frame (5 s) lead-in. This preserves equal pre-grasp context for every proposal without conditioning the anchor on target-layout success.
### 2026-07-29 13:16 CST — Sparse source-state audit first attempt blocked by proxy handling

- Added `scripts/audit_libero_source_phase_provenance.py` to range-read only HDF5 metadata/actions from the revision-pinned official LIBERO drawer task and match them by exact action hash to the compact LeRobot bank; image datasets are never requested.
- The first invocation read no source data because `fsspec`/`aiohttp` did not inherit the workstation HTTP proxy and timed out, while `curl` to the same pinned URL succeeded and advertised byte-range support. Enabled `aiohttp` environment-proxy discovery and will make one bounded retry before deciding whether sparse source-state recovery is practical.

### 2026-07-31 10:38 CST — Action-intrinsic phase bank passes; sparse source-state route rejected

- The completed raw scan `local/phase3b_stage_a/drawer_phase_scans/drawer_phase_20260729T050742Z` evaluated all 36 drawer proposals from the exact v32 failed normalized state under one predeclared rule: retain 50 source-action frames before the first gripper-close transition, reach the corresponding target-layout end-effector pose through the fixed `150/120/70` clearance bridge, then replay the suffix.
- Ten of 36 proposals reach the drawer goal (`694, 560, 737, 489, 462, 414, 523, 738, 538, 711`) where complete-trajectory coverage was `0/36`. Ten proposal-specific bridges fail endpoint/safety validation and remain recorded as failures; no attempt is omitted. Root and normalized-root certificates are exact, and the raw manifest/anchor inventory are hashed.
- The result is enough to prove a phase-conditioned feasible continuation exists for the failed state, but the 50-frame boundary is an action-intrinsic heuristic rather than a source semantic event. Drawer coverage across closed/full and open/sliced execution modes must not be interpreted as one homogeneous aperture effect.
- Tried to recover exact source event labels by TLS-verified byte-range access to the revision-pinned official 1.0 GB task HDF5. Proxy and local cache API issues were repaired, but the HDF5 metadata/action layout produced no inventory after more than nine minutes of sparse reads. The process was terminated with no retained partial download; downloading the full image-bearing file is not justified for Stage A. Removed the exploratory source-audit script rather than publishing an unusable tool.
- Next: freeze the heuristic and its limitation in a v33 contract, raise the native horizon for grasped/open composed plans, implement exhaustive phase-conditioned drawer attempts only for open roots, and require a hard open/grasped/layout-B smoke before the fresh 32-cell run.
- 2026-07-31: Implemented the v33 drawer feasibility repair without touching v32 or any canonical rollout. The fixed 50-frame, first-close action-derived continuation and certified three-leg bridge now apply to **all drawer apertures**, not only already-open roots. This removes the aperture-by-execution-mode confound identified in the diagnostic design; cabinet proposals remain full-trajectory replays and cross-goal oracle comparisons are explicitly a non-estimand.
- The source proposal inventory and full action hashes remain unchanged. Each layout gets a deterministic prefix-replayed anchor bank, while every attempt records the source identity, suffix boundary/hash, layout anchor, bridge result, execution mode/contract hash, and selected cost. A review removed an initial extra full-trace preparation probe before simulation: the shared root is now prepared by normalization alone, with `source_proposal_replayed=false`, so logical proposal-search work exactly equals actual simulator work.
- v33 config validation locks the action rule, bridge route, tolerances, workspace, and all-drawer applicability. Worst-case native horizon preflight is now `2,044` steps; the configured horizon is `2,200`. Static compilation and 22 focused Stage A tests pass, including first-transition slicing, mixed-mode rejection, and normalization-only accounting. No physical smoke or new candidate artifact has yet been launched.

### 2026-07-31 14:10 CST — v34 cabinet phase smoke passes with a one-proposal basin

- Completed the cabinet-only v34 smoke for the exact first v32 open-drawer failure in raw run `local/phase3b_stage_a/phase3b_stage_a_20260731T035449Z`. The process was externally interrupted after proposal 27; the same source/contract/root-bound checkpoint resumed at proposal 28, so none of the completed attempts was simulated twice.
- All `46/46` fixed action-derived bridges pass from normalized state `1a197793…e84a`; exactly one cabinet continuation succeeds (`1/46`, episode `474`, proposal index `31`). It reaches the cabinet predicate at source frame `85`, never reaches the drawer predicate, and never terminates before success. The complete checkpoint is hashed as oracle `86e95718…45ff` under contract `f64db7b…527e`.
- Episode 474 closes its gripper at frame `32`, so the complete-inventory boundary rule retains frames `1+` (`31` realized pre-grasp frames). The result is therefore not a semantic event segmentation claim: a certified bridge to the post-frame-0 source pose changes the same normalized root from `0/46` under full replay in v33 to a nonempty but extremely narrow cabinet basin in v34.
- Decision: v34 passes the cabinet feasibility smoke, but proposal identity remains a dominant nuisance. Before promoting the candidate, run the independent raw-only geometry-feedback cabinet smoke to distinguish physical reachability from open-loop trajectory-basin compatibility; then execute the drawer bank while reusing this complete cabinet checkpoint.
- The first independent feedback smoke, `local/phase3b_stage_a/feedback_reachability_smokes/feedback_20260731T061110Z`, failed safely before bowl contact: the approach descent exhausted its fixed 70-step bridge budget at `47.68 mm` position error, with no goal, termination, or premature grasp. This is an endpoint-budget failure and cannot be interpreted as physical infeasibility. The failed raw manifest is retained; one bounded v2 retry keeps the target/template/tolerances fixed and raises only the final feedback-descent budget.
- The locked retry `feedback_20260731T061506Z` raised only that descent budget from 70 to 170 steps and converged to the same `47.71 mm` residual (`47.68 mm` before). The controller was active at every step and still had no goal, termination, or grasp, so the failure is a geometric/operational-space stall rather than insufficient time. Episode 474's certified v34 phase continuation nevertheless reaches the cabinet from this exact state, proving the state is physically feasible while the naïve vertical feedback planner is not.
- Engineering decision: do not replace one trajectory-compatibility confound with an unvalidated feedback-planner confound. Both raw failures remain immutable diagnostic evidence, but the exploratory smoke script was removed rather than published as a usable oracle. Stage A proceeds under v34's fully certified paths; the future subskill-factorized method must separately certify approach-path planning, contact acquisition, and placement instead of calling any one controller “ground truth.”
- Re-entered the same v34 candidate without `--oracle-goal`. The runner evaluated the 36-drawer bank, reused all 46 complete cabinet results from the validated checkpoint, and promoted the first missing open state without replaying its cabinet attempts. Drawer coverage is `10/36` (selected episode 694, proposal index 1); cabinet coverage is `1/46` (episode 474, index 31). Both share normalized state `1a197793…e84a`.
- Candidate `stagea__drawer-open__possession-on-table__locus-drawer-side__support-demonstration-near__layout-a` is now complete in `phase3b_stage_a_20260731T035449Z` with immutable state hash `e35d6683…d80d`, passing root validation, the full-runtime repeated-action certificate, and the policy boundary. The common support bank remains `456f9c96…c07f39`; no VLA or canonical rollout was loaded.
- Implemented the non-duplicative consolidation contract while the smoke ran. It assigns all 16 closed roots to v32, the already completed open hard pair to v31, and exactly 14 missing open roots to v34. The selected source contracts are byte-identical for construction, demonstrations, support, certificate, factor lattice, and validation; only the later oracle horizon differs (`1850/2200`) after the fixed root timestep 540. Every support pair remains within one source contract, and cross-execution-mode proposal effects are explicitly non-estimable.
- Added pure consolidation validation, an additive in-memory migration for legacy full-replay provenance (raw records remain untouched), an exact/disjoint source-assignment gate, compact report/consolidator and independent verifier scaffolding. The consolidation config assigns `16+2+14=32` candidates exactly; 26 focused Stage A/consolidation tests and compilation pass. The report cannot be generated until the remaining 13 v34 candidates complete.

### 2026-07-31 11:43 CST — v33 causal smoke fails at the untouched cabinet oracle

- Fresh raw run `local/phase3b_stage_a/phase3b_stage_a_20260731T025222Z` tested only the exact first v32 failure candidate. The layout-A phase-anchor bank completed with hash `a7dca541…ce02`; the normalized state hash is `1a197793…e84a`, exactly matching the completed drawer-phase scan root.
- The runner advanced through the new drawer bank, then failed closed because **0/46 cabinet full trajectories** reached either goal. Every cabinet attempt shared the exact normalized-state and normalization-action hashes, stayed nonterminal, and never reached the wrong drawer goal. The full 46-attempt error ledger is retained in the failed manifest/error record; candidate count remains zero and no state was promoted.
- This broadens the falsification: phase-agnostic replay is not merely incompatible with an already-satisfied drawer macro. A cabinet-placement trajectory bank can also have zero coverage after an aperture intervention, even though the cabinet goal itself is unchanged. v33 must not expand.
- Engineering review also exposed that candidate-level atomicity discards a completed first-goal ledger when the second goal fails and offers no proposal-level resume. v34 will use one generic action-derived phase/bridge algorithm for both goals and add validated per-proposal checkpoints before another production candidate. The completed exact-root drawer scan will be reused for diagnosis; it will not be repeated merely to rediscover `10/36`.
- Implemented the v34 offline repair. Drawer and cabinet now use the same source-action rule, layout-specific EEF anchor construction, certified three-leg bridge, normalization-only root, exhaustive selection rule, and per-attempt accounting. Per-goal checkpoints atomically persist each full result and are bound to candidate root, contract, selection lock, source inventory, and execution-contract hashes; a goal-only smoke can later be resumed into the full candidate without repeating completed proposals.
- The first generic preflight caught that cabinet episode 438 closes after only 28 frames, so a literal 50-frame lead is not defined for the complete bank. The locked rule is now outcome-independent but capped: retain up to 50 frames before the first close while leaving at least one source prefix action to define the anchor. This leaves all prior drawer boundaries unchanged and includes short cabinet traces without post-outcome filtering.
- v34 compilation, 23 focused tests, both complete proposal inventories (`36/46`), and horizon preflight pass (`2,044` required; `2,200` configured). The next action is a cabinet-only exact-root causal smoke; its proposal checkpoint will be reused if it passes.

### 2026-07-31 15:18 CST — v34 layout replicate failure and registered-alignment localization

- Resumed raw v34 run `local/phase3b_stage_a/phase3b_stage_a_20260731T035449Z` without rerunning its completed first candidate. The next layout-B root reconstructed exactly (`f2496d6f…c792`; normalized `43226d97…f6d6`). Its drawer ledger completed with `18/36` successes and hash `ca014a1b…98f7`; its cabinet ledger then exhausted all `46/46` proposals with zero successes, no wrong-goal event, and no early terminal event. The exhaustive checkpoint remains `in_progress` only because the bank raises before writing a success-dependent completion marker; its contiguous indices `0..45` make it complete negative evidence and it will not be rerun.
- Corrected a provenance overclaim discovered during consolidation review. Historical v31/v32/v34 contracts do not embed the full YAML `construction` block. They agree on the candidate inventory, demonstrations, environment apart from horizon, certificate, goals, support metric, validation, and dataset root, while revision/config/source hashes differ. Candidate records bind actual construction outputs and timestep 540, but cross-aperture construction-version effects remain aliased. The consolidation validator now treats the artifact as an exact observed-root bank, rejects cross-source causal aperture claims, validates each source record/state/action identity, and reports the historical configuration-binding gap.
- Downloaded only the official 19.1 MB episode-474 main-camera shard into ignored `local/training_data` to investigate source-init provenance. SHA-256 is `decc68b9…419`; the linked-object checksum matches. State and pixel matching both rank init state 46 first, but the match is neither exact nor unique enough to identify the source initialization. This negative result closes the shortcut: v35 may use a declared reference layout, not claim recovered source-demo geometry.
- Ran the four-condition, raw-only diagnostic `local/phase3b_stage_a/layout_alignment_diagnostics/layout_alignment_20260731T070614Z`; both exact roots and three-action certificates pass. Layout A with its world anchor succeeds at frame 85. Layout B with its world anchor reaches the bridge and a stable grasp but fails after all 102 suffix actions. Translating the layout-B anchor by `[-18.41,-17.26,+0.17] mm` (`25.24 mm` norm) to preserve the layout-A bowl-relative offset changes the same suffix to success at frame 80. A fourth branch reuses the registered acquisition snapshot and independently passes grasp-preserving lift, transit, descent, and release.
- Added an integrity-checked compact report at `reports/phase3b_stage_a/layout_alignment_20260731T070614Z/`, a pure fail-closed report builder, and tests. Compilation and 34 focused Stage A/alignment/consolidation tests pass. The result is explicitly exploratory and one-cell only; the next implementation must freeze an additive object-relative certificate on untouched states, reuse completed v34 ledgers, and avoid interpreting cross-certificate proposal rates as one estimand.

### 2026-07-31 15:49 CST — v35 registered rule passes prospective held-root smoke

- Froze `configs/phase3b_stage_a_v35.yaml` before touching the two untouched transverse roots. Every proposal anchor is constructed in canonical layout A and translated only by the target layout's bowl landmark; orientation is unchanged, the target normalized bowl must match within `1e-9 m`, and the rule applies to both goals and layouts. The v35 completion selection is exactly the 13 open candidates not already promoted by v31/v34.
- Raw smoke `local/phase3b_stage_a/registered_generalization_smokes/registered_smoke_20260731T073347Z` evaluates only cabinet episode 474 (full-bank index 31) on the transverse-low-support layout-A/B roots. Both root certificates and bridges pass with zero bowl drift, preserved open drawer, no wrong goal, and no early terminal. Layout A reaches the cabinet at source frame 83 (`623` executed composed steps); layout B reaches it at frame 79 (`619` steps).
- The A→B landmark translation is `[-16.93,-3.88,0.00] mm` (`17.37 mm` norm). This is distinct from the `25.24 mm` registered-versus-v34-world-anchor displacement in the discovery diagnostic. The support bank hash `456f9c96…7f39` matches v34, and the new root hashes are `af2e6ead…d8ae` / `f7ba460b…545d`.
- Added fail-closed completion/import helpers, goal-specific oracle comparability, registered-bank tests, and the compact report `reports/phase3b_stage_a/registered_smoke_20260731T073347Z/`. The full attempt payloads retain production proposal index 31 and may be imported after exact root/execution-contract validation, so the smoke will not be simulated again in the 13-candidate shard.
